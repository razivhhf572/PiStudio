## v0.7.6-beta - 2026-09-06

### 🚀 新功能
- **DSH 插件市场** — 在 DSH 设置里直接从精选目录（awesome-dsh-plugin）安装社区插件（pnpm 安装、热加载生效、实时进度）；「已安装」子 tab 展示已装插件（激活状态/卸载/无设置界面标注）。
- **DSH MCP 服务器管理** — 新增 MCP tab（兼容层让 dsh-mcp-manager 类插件在 headless host 上运行）：列表/添加（HTTP 或 stdio）/启停/删除/连接测试；已装插件重启 host 后自动恢复加载。
- **DSH 技能管理** — 新建/编辑/删除用户级技能（~/.dsh/skills），Markdown 编辑器；保存即生效，composer 里 /技能名 立即可调用。
- **Windows 静默安装** — 插件市场安装不再弹出 cmd 窗口（windowsHide 补丁，postinstall 脚本持久化）。

### 🐛 修复
- **市场安装超时** — 安装/卸载写操作超时放宽到 10 分钟，长 pnpm 构建不再触发 30s 桥超时。
- **重启后已装插件丢失** — 市场装的插件重启 host 后自动恢复加载（bundle 恢复），不再静默消失。

## v0.7.4-beta - 2026-09-04

### 🚀 新功能
- **Command Code 用量查询支持** — 新增 commandcode-credits 解析器，读取 /alpha/billing/credits 接口展示 5h / 周 / 月度三窗口用量；月度窗口按官方定价表反查 5h/周 cap 组合并双重校验（cap 与套餐一致 + 剩余不超上限），校验失败降级只显示剩余额度（fail-closed 防臆造分母）。
- **应用更新生命周期加固** — 更新服务重构为 automatic（electron-updater 自动下载安装）/ manual（macOS 无签名构建仅检查并引导手动下载）双交付模式；清理 stale updater 引用，安装前置退出准备与超时恢复，更新流程更稳。
- **更新源镜像选择与自动体检** — 更新设置支持切换 GitHub 官方 / 内置镜像（ghfast、ghproxy.net、ghproxy.cxkpro）/ 自定义镜像前缀，运行时生效无需重启；打开设置页自动体检镜像可用性与速度（可用 / 较慢 / 不可用），支持手动重新检测。

### 🐛 修复
- **工具耗时秒表不再反复归零** — 耗时计时改为以工具 meta.startedAt 为起点（与结束时 durationMs 同一基准），长命令流式输出期间耗时不再回跳。
- **视觉桥模型选择按钮适配超长模型名** — 超长 provider/model 名在按钮内省略号截断（悬停显示全名），不再顶出布局。
- **统一会话轮次口径** — pi 会话「N 轮」按发言权周期计数（连发 user 消息合并为一轮）；DSH 沿用官方 sessionStats 口径，投影未到时对齐 dsh-web；用量查询的「轮次」改名为「调用次数」，避免与会话轮数混淆。
- **fork 标题持久化与侧栏长名滚动** — fork 会话标题在重启后保留；侧栏超长标题支持悬停滚动查看。
- **Git 角标跨会话保留** — 远程领先 / 落后角标按项目 + 仓库范围缓存，切换会话 Tab 不再丢失（缓存秒显，后台自动校正）。
- **标题滚动更从容** — Tab 与侧栏标题滚动改为慢速线性匀速（时长上限翻倍），当前选中项不再滚动，标题保持可读。

## v0.7.3 - 2026-09-03

### 🚀 新功能
- **聊天系统归档** — 会话可归档收纳，归档界面支持批量删除，工作区不再被旧会话堆满。
- **当前 Tab 始终可见** — Tab 溢出时当前会话 Tab 自动滚动到可视区中央，随时能看清自己在哪个会话。
- **默认模型优先级精调** — 草稿默认值顺序：显式默认 > enabledModels > 欢迎偏好 > 上次使用 > 空；欢迎偏好经目录校验后再生效，思考级别一律取默认档位。
- **DSH runtime：开发用项目依赖、打包保持精简** — dev 模式直接用仓库 @deepseek-ai 依赖（零下载）；打包版不再内置 runtime，需要的人按需安装，不用 DSH 的用户不承担下载体积。
- **Rewind 检查点** — 完整检查点链路：弹层、抽屉面板、时间线回退、自动打点、会话 fork 回退；检查点列表分页，长会话也能回看。
- **DSH runtime 本地导入** — 支持从已解压目录导入 DSH runtime，不必每次重新下载。
- **自动会话标题** — 新会话自动生成标题，Agent 中断后也会补写，侧栏不再全是「新会话」。
- **闲置 Agent 自动释放** — 后台定时回收闲置 runtime，降低常开多会话的内存占用；checkpoint 加载同步加速。
- **用量行常驻与模型卡片收紧** — 内置用量模板自动识别后隐藏配置入口，用量行常驻；Pi 配置「模型」页把模型数量和用量收进卡片标题行，不再多占一条底栏。
- **问题反馈带项目上下文** — 反馈页附带项目环境与日志统计，排障不用再手工拼信息。
- **Ask 通知直达会话** — 后台 Ask 完成通知可一键「前往会话」。
- **配置管理深链** — 深链可直达配置管理的 Pi / DSH 后端分页。
- **beUI 全面启用** — 界面全面切到 beUI 组件；侧栏为运行中会话加上标记。
- **子代理与会话分段卡** — 内置 pi-subagents 扩展读取子代理记录并探测失败态；待办 / 子代理 / 文件修改收成统一分段卡，历史会话也能看 todo 快照。
- **pi-tui 重命名同步** — 在 pi-tui 里重命名会话后，刷新项目或重启会话即可同步到 PiStudio 侧栏，不再卡在旧标题。
- **ask_question 多选** — 提问卡片支持多选并集中提交。
- **按需快速打包** — `dist:fast` 可指定 portable / zip / nsis，本地验证安装包更快。
- **DSH 内置运行时版本探测与 worktree 删除淡出** — 内置 DSH 运行时显示检测到的版本号；删除 worktree 改为淡出动画，不再瞬间消失。
- **模型目录更新功能** — 设置页支持从 GitHub 拉取最新模型目录覆盖内置资源，一键还原、容错回退；配置了自定义认证头时探针跳过自动附加 Bearer。
- **长会话轮次化内存治理** — 历史浏览统一轮次协议：轮次按发言权周期计数（连发 user 合并为一轮）、磁盘分页预取缓存、历史轮折叠时卸载执行过程内容、下线时间线压缩摘要卡片；上下文占用不再封顶 100%，与 pi CLI 口径一致，长会话浏览更流畅。
- **会话 Tab 按项目分组整组重排** — 新会话自动归入其项目组尾（不再追加到全局末尾），项目组之间以分隔线标记。
- **项目显示名重命名** — 项目支持设置显示名（仅改 label，不动磁盘目录），侧栏更易读。
- **fork 会话标记** — fork 出的会话标题带 `(fork)` 标记（物理写入会话名），重命名/删除后不再复现；fork 后旧会话确认无残留运行态。
- **会话文件列表打开文件操作** — 会话修改文件列表新增「打开文件」操作入口，一键直达。
- **模型拉取与测连支持显式代理策略覆盖** — 模型列表拉取与连接测试可显式指定代理策略，不再受全局配置绑定。

### 🐛 修复
- **刷新过期项目** — 刷新会处理已失效项目，文件删除失败会明确提示，不再静默失败。
- **选中态背景恢复** — @theme token 自引用覆盖 foundation :root 真实值，曾导致 bg-accent / bg-bg-active 全部透明；改为 @theme inline reference 后，Tab 激活、侧栏选中及各主题背景恢复正常。
- **dev 不再提示下载 DSH runtime** — 开发模式隐藏安装/重装按钮，仅打包版提供按需安装流程。
- **DSH runtime 安装不再卡主进程** — 安装 / 卸载改走异步 fs，卸载补上进度动画；顺带收紧 multi_select 白名单与 markdown 排版。
- **模型测试连接超时** — 测连写入 stdin EOF，老版本 pi 优雅降级，不再一直转圈。
- **上滑读历史不再跳到最新一轮** — 扩窗补偿改走 restoreAt，视口停在正在读的位置。
- **DSH 工具卡详情文案** — 展开区与 PI 共用整理后的详情文案，不再两套口径。
- **输入栏缩放不再压扁待办条** — 输入栏改为列内固有高度，窗口缩放后待办条保持可读。
- **API key 操作按钮对齐** — 认证页密钥操作按钮对齐，不再错位。
- **通用瞬态故障重试** — 内置扩展覆盖 pi 重试名单漏掉的空响应 / 瞬态错误，偶发中断更少。
- **会话定位轴更跟手** — 刻度更新更轻、跟随滚动、分屏各栏隔离，流式输出时不再卡顿。
- **代理模型选择稳定** — 经代理选择模型时不再抖到错误项。
- **模型探针与 DSH runtime 提示** — 放宽探针超时；DSH runtime 安装进度和失败原因写清楚。
- **Git push / pull 不再卡死** — 推送和拉取按钮卡住时可以恢复，不再假死。
- **历史会话指示高度** — 侧栏历史会话指示条高度修正，不再挤占标题。
- **侧栏 worktree 标题重叠** — 工作树标题与操作按钮不再叠在一起。
- **Pi CLI 更新提示锚点** — 更新提示贴到控件区，不再飘到别处。
- **DSH 默认模型目录恢复** — `settings.describe` 的 base 字段跨层转发，默认模型按 base → schema default 重新列出来。
- **Linux 包保留 DSH sharp** — Linux 安装包不再裁掉 sharp / libvips，DSH 相关能力可启动。
- **加载 Spinner 动画统一** — 加载态改用共享动画工具类，转圈不再偶发静止。
- **子代理记录不丢** — start 锚点持久化，被重启终止的子代理记为 stopped；全量条目表扫描，fork 旁支记录不再丢失。
- **工具结果可打开文件** — 工具输出更精练，并支持在工作区打开对应文件。
- **扩展贡献的模型可选** — 模型选择器 / 测试连接 / Git 提交信息生成默认加载扩展（失败自动降级为无扩展重试），CLI 可选的扩展 provider 模型在桌面端同样可见可选。
- **检查点列表不再显示「暂无」** — 读取改为单次 `git cat-file --batch`（SHA 走 stdin，无命令行长度限制），修复仓库检查点 refs 积累数百条后列表恒为空的问题；检查点面板补充手动刷新按钮。
- **启动 pi 不再闪 CMD 黑窗** — Windows 下 pi 子进程隐藏方式启动，打开会话时不再弹出控制台窗口。
- **用量探测失败可排查** — 记录每次失败尝试（URL/方法/状态码/脱敏响应摘要），全部未命中时在弹窗展示错误详情；New API 等管理面探测不再补 `/v1` 多余尝试，失败原因自动分类提示。
- **Git diff 行内打开文件修复** — diff 更新后点击行内「打开文件」不再无响应，改按快照路径打开 scoped 预览。
- **自动会话标题不再硬截断** — 标题生成避免从英文单词中间截断出碎词；弱回退也不再覆盖用户已有的真实标题。
- **视觉模型被误配为 text-only 修复** — 部分视觉模型不再被当成纯文本模型；测试连接完成后页面滚动位置不再丢失。
- **git 摘要生成套用模型代理名单** — 提交摘要生成进程遵循模型代理名单，代理配置变更后自动重建。
- **git 历史图排除 rewind 检查点** — 历史图不再把 rewind 检查点当作提交展示；生图模式屏蔽安全等级与上下文控件。
- **DSH 删除会话同步清理回收站** — 删除 DSH 会话时把 `~/.dsh/sessions` 对应会话目录一并移入系统回收站。
- **项目行操作按钮重叠修复** — 中等宽度侧栏下，项目行 hover 操作按钮不再叠住项目名。
- **时间线状态跨会话保持** — 执行过程展开/收起状态跨会话切换保持；中断轮只看历史时自动折叠，新一轮开始自动收起。

特别感谢 **微时佬友** 提供的 Grok 模型服务，用于我们的软件开发 🎉

感谢所有群友的建议和问题反馈 🙏

> 💬 **QQ 反馈交流群：1026218644**

## v0.7.2 - 2026-08-30

### 🚀 新功能
- **用量查询重构（对齐 cc-switch）** — 模型 / 认证 / DSH 三处统一的用量显示（卡片右下角金额或百分比 + 头部柱状图入口）；per-provider 启用开关 + 内置模板自动识别 + 通用 / New API 声明式模板 + 超时 / 自动查询间隔（默认 5 分钟，0 = 仅手动）；未启用或不受支持时不再自动查询。
- **多段用量徽标** — 用量支持多窗口分段展示（5h / 周 / MCP、三档百分比）：卡片内全部档位圆点分隔展示，模型选择器行取最严重一段示警；段标签与详情面板共用同一张表，自定义探针窗口名原样展示。
- **DSH 用量查询链路** — DSH 模型配置页同款用量显示与探针配置：配置落 `$DSH_HOME/.pideck/usage-probes.json`，凭据读取 DSH 官方凭据库（`.credentials.yaml`），与 pi 侧同构、互不干扰。
- **用量查询 AI 辅助** — 用量查询弹窗新增 AI 辅助按钮：一键把提示词（走 usage-probe 技能）填入主会话输入框，让 AI 协助查询未内置供应商的用量/余额；无活动会话时改为复制到剪贴板。
- **PiStudio 特有文件统一收拢** — DSH_HOME 下的会话归档 / 宿主互斥锁 / 用量配置统一迁入 `~/.dsh/.pideck/`（一次性迁移，迁移逻辑确认旧布局无残留后随下版删除）。
- **主动更新提示（无配额方案）** — PiStudio 与 Pi CLI 后台每 2h 自动检查更新：启动延迟 30s 首次检查，发现新版本时设置齿轮出现圆点角标并自动弹出更新弹窗（每版本只提示一次，支持「跳过此版本」）；检查源从 GitHub REST API 改为 `releases/latest` 重定向 + `latest.yml`（对齐 electron-updater 策略，不再受 60 次/小时/IP 配额限制，无需任何认证）。
- **发布流程配套 latest.yml** — electron-builder 增加 `publish`（provider: github）配置，`dist:win` 打包后打印需随 Release 上传的资产清单；channel 元数据按平台区分（Windows: `latest.yml` / macOS: `latest-mac.yml` / Linux: `latest-linux.yml`），客户端按平台读取对应文件；缺失时自动降级 atom/API 兜底路径。
- **检查超时防护** — 每次 GitHub 检查请求带 10s 超时：用户本地网络异常时手动「检测更新」最多转圈 10s 即显示失败并可重试，后台检查失败后照常调度下一轮（杜绝历史「一直转圈、手动更新被卡死」）。
- **Pi CLI 后台检查** — Pi CLI 版本随主进程每 2h 自动检查，发现新版本时 toast 提示并在设置页「版本与更新」分区显示当前 → 最新版本。
- **修复手动检测被吞** — 自动检查在途时手动「检测更新」不再被 checking 门控吞掉（历史「有更新也不弹」根因），改为共享同一在途请求。
- **会话定位轴刻度完善** — 会话右缘刻度轴改为覆盖全部已加载消息（去掉 15 条封顶），刻度间距随消息数自适应收缩，空间不足时均匀抽稀并保留首尾刻度；终端坞打开时自动让出底部空间。
- **定位轴分屏让位** — 文件/Diff 工作台分屏打开时，会话右缘刻度轴右移贴齐消息区右缘；solo/最大化视图下回到窗口右缘。

### 🐛 修复
- **更新检查降级路径** — 线上 Release 缺少 latest.yml 时自动降级 atom feed + REST 兜底，不再直接失败。
- **更新检查链路加固** — 仓库坐标统一收敛为 PiStudio 常量（不再依赖 GitHub 改名重定向）；推荐下载资产增加 HEAD 可用性校验，404 时按已知命名变体自动修正 URL，全部失败回退浏览器打开 release 页；版本比较对齐 semver 预发布语义（beta < 同号正式版，beta 客户端能收到正式版更新提示）；新增发版前后端到端自检脚本。
- **DSH 用量查询 provider 名归一化** — `deepseek-official` / `llm-deepseek` 主进程与渲染层统一归一为 `deepseek`，修复模型选择器与圆球面板用量不显示（DSH 卡片行仍显示时的错位）；三处共享同一缓存 key，一处刷新三处联动。
- **文件管理器打开与终端归属修复** — Windows 使用绝对路径启动 explorer.exe 并改用 `shell.openPath` 打开目录（macOS 同步），确保窗口正确前台激活；`activeProjectId` 未同步时终端按钮回退当前会话所属项目，跨项目会话常显「打开终端」。
- **会话面板终端折叠高度同步重构** — 先读取 composer 稳态尺寸再 `setLayout`，修复折叠后高度不释放、输入框悬空等问题。
- **Composer 稳定性** — 输入有内容时 busy 状态保留发送按钮；切换历史后高度稳定；图片生成控件保持单行排列。
- **思考档位选择器不被能力探测阻塞** — 打开思考档位选择器不再等待能力探测。
- **用量显示细节** — 隐藏无余额供应商的空行，调整用量开关上方间距。

### 🚀 新功能

- **模型能力自适应与思考强度链路** — 兼容 pi 0.84.3：端点返回的
  `contextWindow` / `maxTokens` / 思考档位直接作为模型适配依据，能力缓存按
  runtime generation 失效；`get_available_thinking_levels` 经 runtime IPC 按
  `sessionId + agentId + generation + provider + modelId` 隔离缓存（DSH 保持
  独立 reasoningEfforts）；未匹配端点的模型默认开放思考档位，不再一律猜档。
- **模型目录手动刷新** — 模型选择器新增手动刷新，强制拉取模型目录（而非只
  依赖缓存），解决新模型不可见的问题。
- **用量查询扩展** — 内置支持 OpenRouter、Moonshot-Kimi 与通用 OpenAI 兼容
  网关的用量/余额查询，支持一键生成 usage-probes 配置文件；新增 `usage-probe`
  技能与 `/skill:usage-probe` 入口，自定义供应商可自行编写 probes 配置；
  探针卡片额外展示余额/Boost 点数。
- **会话重启与重新加载** — 会话右键菜单与标签页新增「重启」与「重新加载」，
  修复终态 Agent 无法再发起运行时命令的问题。
- **DSH 会话头 Agent 模式胶囊** — DSH 会话头部显示 Agent 模式胶囊：草稿期
  可选择，激活后只读展示（agentPresetLocked）。
- **内置扩展 pi-deck-retry-no-body** — 空响应错误复用 pi 的重试机制，减少
  偶发空 body 导致的会话中断。
- **Ask 并行问询携带主会话上下文** — 并行问询读取主会话上下文，答案可带回
  主线（引用进输入框），后台问题与主线对话保持同步。
- **侧栏 Chats/项目分段** — 侧栏区分 Chats 与项目，新增「新会话」菜单与
  会话搜索，导航更快。
- **会话右缘刻度定位轴** — 会话右缘 beUI PreviewRail 刻度轴，映射时间线
  位置，点击即可跳转到对应消息。
- **侧栏底栏主题循环切换** — 浅色 → 暗色 → 跟随系统一键循环，底部 beUI
  Dock 承载。
- **全局通知升级为卡片 toast** — 系统通知投递升级为自定义卡片 toast。
- **JetBrains 编辑器目录扫描与系统文件管理器检测** — 更深层的 JetBrains
  编辑器目录扫描，「在编辑器中打开 / 在资源管理器中显示」自动检测系统文件
  管理器。
- **内置图片生成技能模板** — 一键安装内置图片生成技能模板。
- **Markdown 文件链接行号跳转** — 显式文件链接支持 `路径:行号` 跳转；裸盘符
  href 不再被 URL 过滤清空，链接在胶囊外也可点击。
- **DSH 官方 todo 接入待办条** — DSH 原生 todo 接入现有待办条。
- **Ask 提问系统通知开关** — 新增独立的 Ask（并行问询）系统通知开关，后台并行问题可以保持静默。
- **引导页支持 @ 文件引用与默认预选** — 新会话引导页支持 `@` 文件引用，并预选默认模型与思考档位。
- **用量探测多账号与独立端点** — 用量探测支持多账号/多角色额度查询（如智谱），并支持按供应商配置独立端点。

- **DSH 双 Agent 后端** — 同一项目下 pi / DSH（DeepSeek Harness）会话并存、自由切换；
  DSH 深融合（utilityProcess 内嵌引导，无 `dsh web`/无端口/无后台 HTTP），懒启动不拖慢应用启动。
- **DSH 会话能力** — 历史分页（`session.history` 事件流翻页，seq 游标）、fork（`session.fork`
  锚 seq 裁剪，fork 点文案回填输入框）、compact（`/compact` 命令）、审批/提问桥
  （`approval/requested` + `question/requested` → 桌面 Ask 弹窗 → `respond` 回执）。
- **DSH 会话持久化** — `dshSessionId` 映射写入 catalog，重启后 attach 恢复旧会话（历史尾部回放）；
  fork/restart 后同步更新映射。
- **DSH 配置管理页** — 设置页新增 DSH 分页：settings/credentials 可视化编辑（schema 驱动表单）、
  host 级模型目录、host 状态与重启。
- **v2 传输形态** — DSH host 从主进程内嵌迁入 utilityProcess（MessagePort fetch 桥 +
  `AbstractApiClient` 契约），原生 ABI 与崩溃面不污染主进程；hostEntry 独立构建产物并
  asarUnpack。
- **后端标识与能力集** — 会话带 `backend` 标识（缺省 pi，旧数据零迁移）；UI 按能力声明
  禁用入口（DSH 隐藏编辑/删除历史消息，保留重发/fork；compact 常显）。
- **DSH 命令补全接入 host 注册表** — 新增 `pideck-command-bridge`（host 进程内桥），
  Composer `/` 菜单从 `ctx.commands` 实时枚举命令（含用户/插件注册的命令），
  会话未激活时自动降级为静态建议集。
- **DSH 会话导出 HTML** — 投影式导出：从 host 分页拉全量历史渲染自包含 HTML
  （dsh-web 视觉的静态内联样式），与 pi 的 `export_html` 同协议返回文件路径；
  活跃会话直接导出内存消息，历史会话自动分页收集（上限防失控）。
- **DSH 技能目录呈现** — 接入 `skill.list` 只读目录：会话工具面板新增「技能」tab，
  展示名称/描述/用途与 `/name` 斜杠调用提示（仅用户可调用的技能带徽标）。
- **当前有效计划 Todo 扩展** — 内置 Todo 支持显式替换和恢复计划、按分支持久化，
  旧任务只会在 Agent 显式清空时移除。
- **模型规格按 listing + pi-ai 补全** — 拉取模型列表时，端点返回的
  `contextWindow` / `maxTokens` 直接写入；缺的按 `@earendil-works/pi-ai`
  内置目录匹配（与 DSH 同源）。匹配不到就留空，不再猜 128k/8k。
  OpenRouter/models.dev 的 SQLite 规格表（`model-specs.db`）已移除。
- **DSH 供应商重试次数** — DSH 没有全局重试项。DeepSeek / OpenAI 兼容供应商的
  「自定义设置」里可改 `retryPolicy.maxRetries`（默认 5，只对瞬时错误生效）。
- **DSH 0.1.0-rc.8** — `@deepseek-ai/dsh*` 升到当前 harness 发布版。本地
  `dsh-tool-pwsh-persistent` 插件继续保留：官方持久 PowerShell 仍走
  `ctx.terminals`、工具名是 `pwsh`（会和一次性沙箱工具抢名字），默认预设也不挂。
- **DSH 0.1.0-rc.7** — `@deepseek-ai/dsh*` 升到当前 harness 发布版（插件可注册
  设置卡片、Codex/Claude Code 子代理进 Job Panel、MCP/ACP 持久图片附件、
  大历史分页栈溢出修复、max-tokens 截断后会话可继续）。英文内置预设
  `Code mode` 跟随上游改为 `PTC mode`。`dsh-bill` 升到 0.13.1。
- **Web 端 dsh-web 风格换肤** — 内置 Web 服务 UI 按官方 dsh-web 设计 token 重构：
  语义色板（背景分层/边框层级/文字层级/状态色）与字体栈取自官方产物，暗色近黑
  分层；侧栏/头部/时间线/Composer 结构精修（毛玻璃头部、聚焦品牌环、内容列宽、
  细滚动条）。
- **Web 端 DSH 工具面板** — DSH 会话头部新增「DSH 工具」入口：目标（只读呈现）、
  子代理（目录 + 展开 transcript）、技能、插件四 tab，全部走 REST（与桌面 IPC 同源）。
- **Web 端 DSH 插件管理** — 动态 Cordis 插件清单 + 安装表单 + 运行/停止/两步卸载
  （G13 语义：进程内临时、按会话归属、面板手势免审批），静态 Loader 只读清单。
- **Web 端双后端徽标** — 侧栏会话行与会话头部显示 pi/dsh 后端徽标（与桌面
  SessionBackendMark 同源），双后端会话一眼可辨。

### ✨ 交互优化

- **会话历史运行时操作遮罩** — 对历史会话执行运行时操作时，消息区域显示
  加载遮罩，避免误操作与重复点击。
- **Tab 栏操作 loading 状态** — 修正 Tab 栏操作的 loading 显示与会话重载
  逻辑，操作反馈更准确。
- **模型能力解释卡移除** — 移除模型能力解释卡片，自适应结果直接填入输入框，
  减少一步确认。
- **设置页分组调整** — 缓存与日志设置移到「开发设置」分组，设置项归类更清晰。
- **Tab 栏运行控制收敛为 ⋯ 分组菜单** — 运行控制上收到分组 ⋯ 菜单，会话
  Tab 栏更清爽。
- **草稿纸面板重排与编辑器选择弹框重做** — 草稿纸面板重新排布，外部编辑器
  选择弹框重做。
- **时间线过程层文字降浅** — 过程层文字改用更浅的 `text-faint` token，
  工具卡片同步打磨。
- **移除启动自动更新检查** — 应用启动不再 ping 更新端点，启动更轻快。
- **持久 PowerShell 终端增强** — 持久 `pwsh` 工具保留多行命令换行（ConPTY
  吞 LF 修复），经 winget/WindowsApps 解析 PowerShell 安装，注入 git/npx
  防交互环境变量。
- **侧栏展开会话折叠控制** — 侧栏展开的项目会话可单独折叠/展开，长项目树更易扫读。
- **模型默认折叠改为派生状态** — 模型目录默认折叠改为派生状态，异步加载期间不再闪开、折叠失效。

- **DSH 会话历史导出路径一致** — 侧栏/抽屉「导出 HTML」对 DSH 会话生效，
  不再提示「暂不支持」。

### 🔧 性能优化

- **终态会话释放运行时状态内存** — 终态会话释放运行时状态内存，降低驻留占用。
- **导出防失控** — 历史导出分页上限 + 超大图片跳过（防生成百 MB HTML）。

### 🐛 Bug 修复

- **未启动会话编辑与内置技能自动安装** — 修复未启动会话的编辑操作，以及
  内置技能自动安装流程。
- **视觉桥配置错误时图片处理异常** — 视觉桥配置错误时不再抛出未捕获异常，
  同时修复 Git 操作超时问题。
- **官方 provider 新模型选择误报** — 官方 provider 新增模型选择失败时不再
  误报「未配置」。
- **审批卡片文本无法选中** — 修复审批卡片中文本无法选中的问题。
- **会话异常状态日志补全** — 补全会话异常状态的 applog 记录，便于排查。
- **窗口置顶按钮状态同步** — 置顶按钮同步主进程真实置顶状态，避免与实际
  状态脱节。
- **文档全屏切换崩溃** — 修复文档全屏切换时 composer 面板 `Group not found`
  崩溃。
- **DSH host 预热与 `/new` 命令** — 优化 DSH host 预热策略，修复 `/new`
  命令被误拦截的问题。
- **背景壁纸 token 注入** — App 背景壁纸 token 注入不再导致主题明暗互相覆盖。
- **文件管理器打开方式与跳转导航** — 修复文件管理器打开方式与跳转导航相关缺陷。
- **刻度跳转在消息部分加载时不可达** — 消息未加载完时刻度轴跳转同样可用。
- **宠物多任务并发状态脱节** — 宠物 failed/review 过渡按业务态重新聚合，
  巡游与业务态互斥。
- **模型保存验证** — 保存模型不再把 config-fallback 当假绿灯。
- **失败类消息诊断卡片** — 失败类消息渲染诊断卡片；重试进度只弹 toast。
- **RPC 日志开关静默失败** — 日志开关不再静默失败。
- **DSH 归档名丢失与跨项目归档视图** — 归档名不再丢失，归档视图不再跨项目全量。
- **打包/启动前先构建本地 packages** — 打包/启动前先构建本地 packages，
  修复 DSH host 缺 `lib/index.js` 启动崩溃。
- **设置弹窗滚动与子会话父关系回填** — 设置弹窗内容可正常滚动，平铺子代理
  父关系自动回填，避免重启后孤儿平铺。
- **DSH 流式图片渲染恢复** — DSH 流式图片可正常渲染（直播流期间保留
  hydrated 图片字节）。
- **普通工具误标 MCP 徽标** — 普通工具不再被误标 MCP 徽标。
- **Markdown `~` 路径被删除线劈开** — `~` 前缀文件链接不再被单波浪线删除线
  劈开，无法解析时提示而非静默打开空文件。
- **DSH 插件配置输入被打断** — 插件配置输入不再中途被打断，插件区排版与
  配置页位置记忆。
- **pi→DSH provider 迁移无需预建条目** — 内置 provider 迁移到 DSH 不再要求 DSH 侧预先存在同名条目。
- **Markdown 文件链接存在性校验** — 消息中的文件链接先校验目标是否存在，失效路径降级为纯文本。
- **关闭确认列出全部未保存变更** — 设置/配置关闭确认逐条列出全部未保存项，字段改回原值时脏标记自动清除。
- **缩放变化保留面板宽度** — 窗口缩放变化时不再跳变分栏宽度。
- **恢复 pi 模型字段编辑** — pi 配置页模型字段可正常编辑。
- **运行时切换与轮次历史对齐** — 切换运行时不再导致轮次历史显示错位。
- **会话删除/归档清理子会话** — 删除或归档会话不再遗留游离子会话，统一退出处理。

- **DSH 注入上下文不再投影为用户消息** — DSH 会话注入的 AGENTS.md / runtime
  context / skills 按 `source.kind` 分流，时间线只留真实对话。
- **文件抽屉滚动容器归属** — 滚动层归属收敛到抽屉容器（同步 dev 基底修复），
  `files-panel` 不再自带纵向滚动。
- **移除失效的「默认展开本轮修改的文件」设置** — 本轮修改文件列表已移入输入框上方并固定默认收起，原「每轮回答下自动展开」开关已不再产生任何效果，连同其设置项与遗留死代码一并移除。
- **打包版终端找不到 pty.node（#154）** — afterPack 重打 asar 时保留
  unpacked 标记，并显式 unpack `node-pty`，避免 `terminal:ensure` 在
  安装包里加载原生模块失败。

### 🙏 致谢

- **@bfzha** — 持久 PowerShell 终端增强、宠物状态修复、DSH 归档/流式图片/
  插件输入修复、Markdown 链接行号跳转与可点击、构建/启动打包修复，以及早前
  一轮：DSH 双后端配置管理/迁移分层、CodeMirror 配置源文件页、外观主题整套
  重绘（经典/森系绿/石墨灰/海盐蓝/暖阳米）、会话划选文本引用 chip、时间线
  滚动锚点跨会话保持（#163）。
- **@ayuayue** — 用量探针余额/Boost 展示、Ask 主会话上下文、侧栏分段与搜索、
  PreviewRail 刻度轴、主题循环切换、卡片 toast、编辑器/文件管理器检测、
  图片生成技能模板、Tab 栏与时间线打磨，以及模型能力管线、用量查询、重启/
  重载、Agent 模式胶囊、retry-no-body、DSH 双后端核心、生图独立后端、
  dsh-web 换肤与 40+ 项修复。

特别感谢 **微时佬友** 提供的 Grok 模型服务，用于我们的软件开发 🎉

感谢所有群友的建议和问题反馈 🙏

> 💬 **QQ 反馈交流群：1026218644**

## v0.7.1 - 2026-08-15

### 🚀 新功能

- **会话轨迹迁入右侧抽屉 + 过程账本** — 思考轨迹从会话中栏切到右侧抽屉独立 tab（复用
  会话消息缓存）；JSONL 过程事件、首轮初始提示与 pi-system 参考进入账本；耗时只显示
  实测或轮内回推区间，不再把时间点写成 0ms。
- **Markdown 增量渲染** — 长流式消息改为增量渲染（IncrementalMarkdownFrontier /
  冻结 chunk），不再每帧全量重解析；高亮 / mermaid / 元素树的全量渲染延迟到空闲时执行。
- **阅读面统一走分屏** — 移除右侧抽屉编辑器面板，文件查看统一走中间分屏
  （SessionTabsBar + WorkbenchContent），不再有抽屉里的旧查看器。
- **mac 自定义标题栏走系统红绿灯** — 标题栏改用系统窗口按钮（hiddenInset +
  trafficLightPosition）；侧栏折叠与会话 Tab 为红绿灯让位。
- **无工作区项目引导** — 侧边栏在没有工作区项目时渲染空态卡片与「添加目录」按钮，
  新用户不再只剩搜索行。
- **Web 端 ask 请求卡片** — 本地 Web 服务新增 `/api/ui-response` 接口，ask 请求
  以卡片形式展示请求快照，SSE 工具循环收尾修正。
- **引导页统一为新建页面** — 无会话空态直接渲染居中输入区（renderer-only 虚拟会话：
  不创建目录记录、不拉起 pi、不占 Tab）；Logo 下方项目名升级为下拉（含全部项目与
  内置 Chat，切换即换发送目标），选中模型/思考级别实时显示。首次发送才创建真实会话，
  composer 状态与已输入消息整体带入新会话——Chat 项目从引导页进入也统一创建可保存的
  普通会话；启动不再自动创建草稿会话。
- **本轮修改文件列表改为纯手动折叠** — 标题行常驻按钮收起/展开整个列表，移除数量
  阈值自动折叠。
- **起始页移除快捷动作按钮** — 起始页输入区去掉快捷动作按钮，输入框高度统一为
  150px。

### ✨ 交互优化

- **桌面宠物按精灵原比例显示** — 宠物按 spritesheet 单格原始尺寸绘制，默认 100% 缩放，
  跟随缩放/字号设置同步更新，并采用高质量插值，不再发糊发小。
- **流式对话默认展开中间过程** — 流式对话默认展开思考/工具执行中间过程；过程安全门
  默认启用但零干预，不打扰只读用户。
- **统一 dsh-web 形态** — 会话待办条、模型/思考合并选择、上下文圆环与状态明细在桌面
  与 Web 端统一呈现。
- **系统通知点击直达对应会话** — 通知点击优先按会话 `record.id` 定位并切换到对应
  会话，不再只落在当前会话。
- **思考块与折叠卡触控交互优化** — 思考块整行可点击展开/收起，带 hover/active 反馈
  与 chevron 旋转过渡；展开态内容底部新增收起按钮，长内容滚到底也能收起；折叠预览改
  等宽字体；Web 端时间线同步同样的触控交互。
- **工具卡 running 去转圈** — 运行中状态移除转圈动画（保留「进行中」文字），状态仍由
  工具执行事件驱动。
- **起始页统一居中** — 无会话起始页的 ComposerArea 统一居中（宽度与引导页一致），
  不再受残留布局偏移。
- **DevTools 快捷键收敛** — 快捷键统一收敛到 `devTools.ts`，webview 内 F12 改为打开
  主窗口 DevTools，不再被内嵌页面拦截。

### 🔧 性能优化

- **流式渲染优化** — 冻结 chunk 引用稳定（不再每帧重建插件）、prefix 字符串缓存（避免
  每帧 slice 大字符串）、settle 全量渲染延迟到 requestIdleCallback，长流式更省 CPU/内存。
- **设置页秒开** — 设置弹窗按 tab 拆分懒加载（首开 chunk 441KB→25KB）；宠物雪碧图改
  pideck-pet:// 协议（manifest 不再内嵌 7.4MB base64 大图）并按 mtime/size 指纹缓存；
  应用日志行缓存，历史文件零重读。
- **流式推送窗口加宽** — text/thinking 推送窗口 50ms→100ms，配合增量渲染进一步
  降低渲染频率。
- **流式事件洪泛内存不回落修复** — 流式渲染改为 O(n) 每帧扫描、全量 IPC 事件按需
  处理，事件洪泛时内存不再 GB 级只涨不回落。
- **不可冻结消息纯文本兜底** — 无法冻结的长消息流式期以纯文本兜底渲染，settle
  全量渲染设上限，打字机空转自动停帧。

### 🐛 Bug 修复

- **customPiPath 失效回退自动检测** — 自定义 pi 路径失效时不再卡死在检测阶段，回退自动
  检测。
- **更新检查改为仅设置页手动触发** — 禁用更新检查后不再自动检测、不再出现 loading 转圈。
- **抽屉切 tab 呼吸式宽度摆动** — 滚动容器统一 scrollbar-gutter: stable，修复切 tab 时
  宽度 310↔320 反复摆动。
- **会话定位悬浮菜单跟随缩放** — 窗口缩放/拉伸后菜单位置跟随（RO 直察 Group 容器回写
  面板像素），侧栏宽度同步修复。
- **项目路径气泡移入不再闪烁** — PathTooltip 关闭指针事件并加关闭宽限期，触发区扩到整行
  选择按钮。
- **git 面板折叠按钮不再偏下** — 折叠头部按实际 h-8(32px) 预算统一，按钮不再被裁切。
- **宠物窗 partition 统一** — 宠物窗 partition 常量统一，精灵协议挂独立 session。
- **流式滚动跟随不误逃逸、读历史不被拽回** — 流式追底存在弹簧滞后时，轻微上滚（含
  触控板惯性/误触）不再被误判为「用户离开底部」（增长守卫带），回复持续贴底滚动；
  真正上滚离开底部读历史后，内容增长不再把用户拽回底部——仅当用户主动下滚回近底带时
  恢复跟随，不再需要手动点「回到底部」按钮。
- **上下文圆环面板滚动重新锚定** — 圆环面板在滚动/窗口缩放时改为重新锚定到触发器
  当前位置（rAF 合并，位置未变不重复渲染），流式追底滚动时不再自动关闭；外点/Escape
  仍是唯一关闭途径。
- **思考折叠单行结束态空白** — 思考步骤折叠为单行时，结束态不再残留空白。
- **添加项目不再被聊天区占用（#149）** — 添加项目入口不再被会话聊天区抢占，始终可从
  侧边栏访问。
## v0.7.0 - 2026-08-13

### 🚀 新功能

- **Session-first 架构（#113）** — 会话成为一等公民：runtime 绑定、流式状态、
  Composer、运行时 UI 全部按会话隔离；围绕 agent 的全局状态迁移为按 sessionId
  键控的 Jotai atom。
- **会话 Tab 栏** — Tab 可固定（pin）、拖拽排序带插入指示线、预览模式（发送后
  自动转常驻）、状态徽章，操作收敛到统一 Tab 下拉（新建会话入口移入项目下拉）。
- **分屏重构** — 焦点驱动视图切换、面板级退出分屏、分屏组胶囊（自定义名称/颜色）。
- **会话分支导航条** — 借鉴 AI Elements MessageBranch 分页器，在分叉会话分支间
  导航。
- **流式渲染重构** — 逐字打字机（useSmoothStream）独立流式通道；思考步骤按
  Markdown 流式渲染；执行过程折叠为 Chain of Thought 步骤时间线；中间回复收进
  折叠区；会话切换保留查看位置。
- **会话文件修改汇总** — 会话结束后在本轮 write/edit 修改的文件汇总到末尾便于审查。
- **换肤系统** — 皮肤预设 + 自定义背景图 + 可切换主题色（warm-white 色板）+ 壁纸
  模式面板/弹窗透明度；内容最大宽度改为会话面板百分比（旧 px 值自动迁移，上限提至
  1800）。
- **编辑器：CodeMirror 6** — 替换 Monaco（产物体积 72MB→39MB）；默认编辑模式、
  自动保存、右键选区引用、图片/PDF 预览；编辑器升级为一等抽屉面板。
- **Markdown：Streamdown 成为唯一引擎** — code/mermaid/math 全部走 Streamdown
  官方插件，移除 react-markdown。
- **Git 行内操作** — 变更列表行内 add/rollback/openfile（VS Code 语义）；
  Push/Pull 状态角标；右键删除（走回收站）；提交摘要生成带进度/超时/防抖；
  diff 渲染迁移 @pierre/diffs。
- **RPC 日志查看与审计** — 实时日志弹窗（自动滚动/搜索/复制/保存到文件）；日志
  查询与审计（logQuery/sharedLogger/trash 审计）+ LogViewer 重构。
- **用量统计** — 设置页用量统计 tab、实时耗时、会话平均缓存命中率；引导卡一键安装。
- **模型管理** — 模型列表实时化（本地 models.json 优先，新模型重启 Agent 生效）；
  模型表新增能力列；分层计费编辑器；会话上下文详情展示延迟指标（TTFT/总时延/
  tokens 每秒）。
- **视觉桥（Vision Bridge）** — 给非视觉模型「眼睛」（工具结果绕行），prompt 模板
  持久化到配置文件。
- **系统通知** — 会话结束与 AI 询问触发系统通知，点击跳转到对应对话。
- **宠物状态提醒改造** — 头顶气泡、字号跟随设置、状态词着色与等待操作提醒。
- **Web 服务：SSE 流式 + React 化** — `/api/chat` 走 AI SDK UIMessageStream 协议
  流式端点；外部 Web 端 UI 与桌面端对齐。
- **文件树拖拽 @ 引用** — 拖拽文件/目录到输入框插入 @ 引用（支持 @directory）。
- **窗口与布局记忆** — 启动窗口大小预设 `last`；侧栏/抽屉宽度、设置页与 Pi 管理页
  Tab 位置跨重启记忆。
- **清理界面本地缓存** — 设置缓存/日志页新增一键清理操作。
- **生图模式** — Composer 切换模式生成图片，复用现有模型配置；beUI 点阵动画贯穿
  生成→完成→失败三态；生图结果作为消息上屏渲染（提示词立即上屏、图片随 assistant
  消息展示）。
- **安全管理** — 等级化工具权限门（pi-deck-security-gate 扩展 + Pi 管理配置 + 会话级
  切换）；运行中可切换思考强度与安全级别（#146）。
- **模型规格自动填充** — 内置 SQLite 规格表（发版前同步）；内置商汤/阶跃星辰模型；
  保存所选模型时按规格表自动补全，去掉硬编码默认值。
- **Web 服务：局域网访问 + 扫码** — 设置页生成二维码，局域网设备扫码即可访问；
  Web 端首页支持直发新建会话（携带模型/思考级别偏好）；Web 面板增加模型列表/创建
  项目/头部侧边栏；会话/项目列表按时间排序，项目会话默认折叠至 5 条。
- **用量统计：按天明细** — 今日概览与按天搜索明细视图；聚合器支持按天模型/项目
  明细，缓存结构升级 v2。
- **审计留痕扩展** — 会话创建/复制/导出/改名与 runtime 全周期、设置/安全敏感操作
  （密钥类值不落盘）、pi 进程 spawn 诊断与 Agent 生命周期退出决策、背景图/git
  初始化/单实例/Web 服务启停、清日志操作均留痕。
- **视觉桥增强** — 转换结果在会话内可见（图片卡片 + 请求详情）；设置页新增超时时间
  输入（默认 30s→120s）；max_tokens 支持「不限制」下拉；保存统一走设置弹框头部。
- **思考折叠单行（deepseek-harness 模式）** — 思考默认折叠为一行：流式中显示最新行并
  带扫光动画，结束后显示第一行并静止（此前跑马灯方案移除）；点击标题行展开全文。
- **回合耗时移位** — 回合总耗时移至回合尾部展示（流式中实时更新），行头仅保留时间。
- **移除发送置顶动画** — 发送消息不再平滑置顶清屏：与流式跟随有冲突、偶发页面抖动，
  回归普通贴底跟随。
- **diff 增强** — 大文件 diff 启用 GitHub 式折叠/虚拟化/worker 渲染；变更视图默认只
  显示变更块；diff 汇总改为每轮会话独立展示（TurnFileChanges，beUI FileDiff 语法
  高亮），移除底部全局汇总；文件修改列表超 3 个默认折叠。
- **超长会话轮次化历史模型** — 12 轮缓存 + 三级分页链路；历史会话可查看完整输出；
  上滑翻页节奏优化；修复编辑/删除/重发定位。
- **并行问询** — 发送行为菜单常显（shadcn 下拉）+ 匿名会话胶囊结果展示；渲染 ask
  提问卡片并支持回答。
- **日志查看器** — 时间区间筛选与分页增强。
- **进程监控** — 展示 agent 关联的会话标题；改用专用内存口径对齐任务管理器；只记录
  pi agent，移除 Electron 自身进程统计。
- **托盘菜单** — 新增「重启」（统一清理后 relaunch + quit）。
- **RPC 日志右键** — 改为「打开RPC日志」，运行中 agent 也可用。
- **保存入口统一** — Pi 管理页/安全管理页保存统一走弹框顶部按钮（未保存修改黄点
  标记 + 关闭前保存确认，移除各 tab 内保存按钮）。
- **公式复制** — 行内公式复制改为点公式本体；常显按钮覆盖行内与块级公式。
- **UI 细节打磨** — 窄侧栏按钮让位、todo 列表按序展示、widget 字号体系与文件行
  hover 过渡；压缩卡片对齐思考卡片视觉、用户长消息自动折叠；会话头显示项目/标题
  面包屑 + TODO 徽章；运行状态指示器改用 beUI ReasoningText；已完成 todo 去掉删除线
  仅保留对勾。
- **Prompt 管理** — 删除 Prompt 增加确认弹框；复制按钮默认复制文本并迁移 shadcn
  DropdownMenu 带动画。
- **其他** — Ctrl/Cmd+点击链接改系统浏览器打开；费用悬停提示人民币换算（估算汇率
  7.2）；启动动画 + AppErrorBoundary 错误上报；会话重启过渡动画；新建会话默认模型
  配置；提问面板可折叠并支持对象选项。

### ✨ 交互优化

- **侧边栏可发现性优化** — 三个点菜单入口、浮层虚化、子项收起。
- **附件选择器** — 默认只选文件；复制图片粘贴改为图片预览。
- **RPC 日志交互打磨** — 会话/agent 同一套右键菜单；未启动 agent 时开关置灰并提示。
- **工具调用时间线** — 紧凑低调化，三态状态徽章。
- **飞书绑定会话** — 禁用 ask 交互提问并提示用户。
- **内容宽度与卡片** — 内容宽度 100% 时保留两侧 24px 空隙；消息与输入框共用宽度
  百分比；mermaid 恢复自然宽度；ask 卡片头部压缩；工具卡片圆角对齐系统 token。
- **技能目录选择器** — 简化路径显示。

### 🛠 架构重构

- **Issue #113 结构重构** — `shared/types.ts` 拆分为 11 个领域文件；tsconfig 拆分
  为 main/preload/renderer 三份；IPC handler 按域抽取（`editorsIpc` /
  `scratchPadIpc` / `projectsIpc` / `storeIpc` / `sessionIpc` / `systemIpc`）；新增
  `agentUtils` / `modelListCache` / `wslExe` 模块；FeishuConnection 从 FeishuBridge
  抽取；App.tsx 通过 10+ 个 hook（useSessionActions / useComposerSend /
  useQueuedPrompt 等）与组件抽取（AppShell / SessionView / SidebarComponents 等）
  大幅瘦身。
- **UI 2.0** — 全应用迁移 Tailwind CSS v4 + shadcn/ui：原生 Input/Textarea/
  Checkbox/button/tab 全部换装；弹窗统一 shadcn Dialog 壳；会话/工作台布局改用
  react-resizable-panels；toast 统一 sonner；清理死 CSS 约 1900 行。
- **Session-first 运行时** — SessionRuntimeInjector 把流式从 App 根隔离；Composer/
  时间线/侧栏/视图由会话持有；旧 runtime 的迟到结果按代际拒绝。
- **打包优化** — 渲染层依赖移入 devDependencies；asar 压缩开满。
- **E2E 基建** — Playwright Electron 测试框架 + mock-pi RPC 全流程（prompt/stream/
  done/abort、排队消息、模型选择、compact/fork、重启后会话可用）。

### 🔧 性能优化

- **流式与内存治理** — 增量消息 flush、去 shiki 高亮、asar store、滚动接管；渲染层
  消息缓存上限 20→8。
- **激活分页** — 只下发最近 3 轮，历史按完整对话轮次分页加载。
- **工具输出截断** — 超长工具结果截断下发，「查看完整输出」按需加载；图片懒解码；
  agent 退出释放缓存。
- **时间线渲染** — `content-visibility` 跳过屏外 layout/paint。
- **会话目录缓存** — 先回显缓存树，后台扫描推送更新；缓存命中率统计文件级缓存并
  并行化。
- **Electron 43 升级与首屏瘦身** — Electron 43 升级 + 内存采样工具链 + 首屏瘦身；
  进程监控改用专用内存口径对齐任务管理器。

### 🐛 Bug 修复

- **「停止」停不下来** — 补发 `abort_bash` 升级 + 用户可见提示。
- **中间回复消失循环根治** — live 挂载点要求活动流 + stopReason 协议判定。
- **启动时模型列表永久为空** — 空结果不再缓存，首次空拉取自动重试。
- **Linux Wayland 会话** — 默认不再强制 XWayland（仅启用宠物时应用兼容层）。
- **删除操作** — 统一走系统回收站并补全审计日志。
- **历史会话重命名/复制** — 改用 pi 原生 session_info 追加格式。
- **Web dev 代理** — 修复空白页与主机列过宽，补齐 HMR websocket 代理。
- **弹框内链接** — 统一强制系统浏览器打开（技能/扩展卡片、诊断文档、环境引导、
  Web 服务）。
- **带空格路径引用** — 粘贴含空格绝对路径可形成完整文件引用；附件选择器默认只选文件。
- **飞书 ask/confirm** — 提问卡片不再阻塞 agent。
- **分屏与 UI 修复** — 分屏最大化宽度恢复、运行时配置更新后底部栏即时刷新、终端
  dock 输入框高度跳变、RPC 日志右键菜单、侧栏草稿右键、未启动 Agent 时输入框
  上下键历史。
- **黑屏治理** — 渲染进程崩溃自动恢复；时间线上滚窗口化。
- **历史加载** — 根治历史重载双份/消息丢失；历史会话打开/切回不再闪新会话起始页；
  起始页骨架屏与淡入动画时机修复；大会话起始页误显根治（compaction 分页坐标错位）。
- **消息编辑** — 编辑窗口外消息时丢弃失效历史前缀，修复「编辑不刷新」。
- **流式与时间线** — live 正文挂载按轮级门控，根治 steer 后同一中间回复前后双份；
  删除/重发墓碑保留 id 与 parentId，避免整页消息被清空；删除 assistant 回答时墓碑
  同一轮的思考/工具链；流式换行改走弹簧追底；自动收起执行过程不再抢滚动；负增长
  重锁加逃逸守卫。
- **扩展管理** — 解析 pi list 的 (filtered) 后缀，修复过滤式安装扩展无法卸载/更新/
  查版本。
- **WSL** — 会话读写 maxBuffer 扩容，防大会话从列表消失（#147）。
- **会话安全等级** — PIDECK_SESSION_ID 改用会话身份 key，修复覆盖失效。
- **损坏会话自愈** — 自动修复首行「路径+header」粘连与旧版 sessionName 私有头行；
  catalog 写盘遇杀软瞬时锁 EPERM 退避重试，不再阻断新建会话。
- **剪贴板与图片** — 剪贴板图片粘贴位图兜底；右键粘贴支持图片（微信复制图片不再变
  @path 引用）。
- **Composer** — 普通 @/& 不再误开建议；粘贴改为纯文本避免光标跑飞；生成中可切换
  模型（本轮结束后生效）。
- **Tab 与分屏** — fork 后切到新会话并登记常驻 Tab；Enter 发送时预览 Tab 自动晋升
  常驻（4 个发送入口统一）；固定 Tab 与普通 Tab 同宽；缩放后抽屉悬浮条跟随；启动
  0↔min 宽度震荡与清缓存后抽屉闪修复。
- **壁纸模式** — 磨砂块透明化；草稿本面板底色跟随系统面板色与背景图透明度。
- **Git** — 修复 Windows 8.3 短路径下 stage/unstage 误报 outside 与 worktree 删除
  失效；未跟踪文件 diff 打不开；手动刷新同步 push/pull 角标；暗色下角标可读；非
  git 仓库暂停定时刷新；stage/unstage 竞态静默跳过 stale 路径。
- **Toast** — 会话失败/重试等异常 toast 常驻不自动消失；toast 提升到 body 修复弹窗
  遮挡。
- **链接** — 官网主页链接强制系统浏览器打开，不受内置浏览器设置影响。
- **pi 运行时** — mise 自定义目录与动态 PATH 支持；npm 检测/安装复用搜索目录。
- **RPC 与进程** — 启动/重连 RPC 超时接上用户配置，失败 toast 带具体原因；手动停止
  后不再发送「已完成」系统通知；首字/总耗时计时起点改为请求发出时刻（思考模式按
  正文首 delta 计算）。
- **模型** — 模型规格匹配与合并语义修复；保存所选模型时按内置规格表补全；修复历史
  模型 5 项回归。
- **其他 UI 修复** — 模板选择器眼睛预览/返回按钮暗色可读；空正文提示词模板发送拦截
  并明确提示；会话管理与导入弹窗复选框可勾选；默认模型/提供商清除只清值不清 key；
  侧栏分区标题行整行可点击；扩展页残留分隔横线移除；宠物窗诊断日志补盲区、渲染失败
  兜底；重建 Windows 图标缓存脚本（任务管理器显示新图标）；Logo 换新为单色版，
  opencode 徽章改中性灰。

### 🙏 致谢

- **@bfzz / @bfzha** — Git 行内操作、分屏、会话 Tab 栏、UI 2.0 迁移及 session-first
  重构的大量工作。
- **@1900EasonJin** — 换肤系统、壁纸透明度、宠物状态提醒、思考流式渲染及终端/UI
  打磨。
- **@qgx1992** — Ctrl/Cmd+点击系统浏览器打开链接。

特别感谢群友 **微时、kylin、Island、PieDriver** 提供的模型服务，用于我们的软件开发 🎉

> 💬 **QQ 反馈交流群：1026218644**

感谢所有群友的建议和问题反馈 🙏

---

## v0.6.7 - 2026-07-29

### 🚀 新功能

- **紧凑标题栏 + Codex 风格右侧栏** — 顶栏更省高度，右侧抽屉 Tab（文件 / Git /
  浏览器 / 草稿本）改为更密的多面板工作流样式。
- **文件编辑器收纳到 Files Tab** — 编辑器 Tab 进入文件抽屉内部，与 Git/浏览器
  面板的 chrome 更统一。
- **文件树拖放 / 粘贴 / 移动** — 支持拖入文件、粘贴文件，以及树内拖拽移动。
- **@ 文件建议支持目录树与搜索** — 文件选择器可浏览目录树并过滤，深层路径更好选。
- **Composer 粘贴 / 拖入路径引用** — 向输入框拖放或粘贴文件可插入路径 chip，
  带空格路径也能正确保留。
- **文本链接默认内置编辑器打开** — 点击文本文件链接走应用内编辑器，二进制仍走
  系统外部程序。
- **批量提问 Tab UI** — `ask_question` 批量模式以 Tab 一次展示全部问题，可选
  Submit/审阅后再提交答案。
- **Ctrl/Cmd+点击 Markdown 链接打开系统浏览器** — 按住修饰键点击时交给系统默认
  浏览器，不再强制内置浏览器。
- **Tailwind CSS v4 + shadcn + sonner Toast** — 渲染进程样式栈升级，通知迁移到
  `sonner` 并适配主题。
- **侧栏项目展开/折叠状态持久化** — 项目折叠状态跨重启保留。
- **会话消息 Fork** — 用户消息操作栏支持从该消息 fork 新会话（对应 pi `/fork`）；
  Agent 忙碌时不显示入口；成功后预填原 prompt 到输入框，可改后重发。
- **启动页官方 pi 拼装动画** — 冷启动覆盖层循环播放与侧栏同源的像素 tetromino logo
  动画（可调大/加速）；PiStudio 标题与副标题改用 Plantin 品牌衬线，气质对齐空状态文案。
- **单实例复用窗口** — 默认开启：再次打开应用会唤起已有窗口（含托盘隐藏），避免多开；
  常用设置可关闭以允许多实例（需重启生效）。
- **启动窗口大小预设** — 外观设置可选最大化 / 全屏 / 大中紧凑窗口；默认最大化
  （与历史 `ready-to-show` 后 maximize 一致，不挡任务栏）。
- **会话压缩可视化配置** — 配置管理 Settings 将 `compaction` 拆为「自动压缩 / 预留回复
  Token / 保留近期 Token」三项，不再整段 JSON 手改。
- **LaTeX / 数学代码块渲染** — 会话内 `latex`/`tex`/`math` fence 用 KaTeX 渲染。
- **Electron Chromium 沙箱开关** — 开发设置可启用渲染进程沙箱（默认关闭，兼容 Windows
  安全软件/旧 GPU）；改完需重启 PiStudio。

### ✨ 交互优化

- **Plan 模式流程打磨** — 计划结束三卡片布局、修订返回按钮、跳过保持只读更清晰。
- **Composer 挂件与扩展 UI** — 扩展挂件固定在输入框上方、高度更紧凑，内置扩展
  冲突提示更友好（含 todo 文案）。
- **上下文压缩入口** — 底栏压缩按钮仅在占用 >30% 时显示；样式与图标更克制，
  「会话太小 / Nothing to compact」类错误改为友好提示。
- **UI 去饱和绿色** — 降低刺眼绿色强调，优化输入栏与状态指示对比度。
- **Worktree 侧栏层级** — 嵌套更清晰、可折叠 worktree 会话、弱化重色块与 active 行
  噪声。
- **扩展安装 / 卸载体验** — 进度更清楚，卸载时本地文件清理更可靠。
- **RPC / Agent 启动选项** — 支持可选 `--no-themes` / `--offline` / `--no-extensions` /
  `--no-skills`，启动时预热版本缓存；开发设置可禁用扩展/技能以加速或排障。
- **文档与社区** — 官网截图更新到最新 UI；英文首页与双语导航扩展；README Star History
  图由 CI 自动刷新；维护者教程视频制作流程补充。

### 🐛 Bug 修复

- **输入框历史 ↑/↓ 丢失半截草稿** — ArrowUp 改为快照 live 草稿（`livePromptByAgentRef`），
  不再使用可能过期的 rendered prompt，ArrowDown 可完整恢复继续输入的内容。
- **Agent 启动防闪退 / 诊断增强（尤其 macOS arm）** — 在 `spawn` 前挂上 pi 生命周期监听，
  默认 `error` sink 避免 ENOENT 等升级成主进程未捕获异常；启动失败写入可复制诊断卡片，
  日志补充 platform/arch 与 child-process-gone；并扩展 macOS pi 搜索路径
  （`/opt/homebrew/bin` 等），缓解 Dock 启动 PATH 不完整。
- **宠物状态卡死在 review/failed/jumping** (#107) — 过渡恢复定时器不再被冷却/重叠推送
  误取消，review/failed 可按时回到 idle。
- **停止后的流式余晖** — abort 后密封 stream generation，延迟 thinking/text 不再混入
  下一条回复；停止反馈改为 toast 而非时间线系统卡。
- **禁用内置扩展后仍加载** — 移除/冲突让位时真正删除用户目录下的内置扩展文件，并清理
  残留，避免与第三方扩展工具名冲突导致 RPC 失败。
- **手动压缩按钮与状态** — 恢复底栏 compact 入口；RPC 使用 `customInstructions`；
  结束时正确清 `isCompacting` 并回 idle，失败原因可 toast 展示。
- **系统标题栏缺少侧栏开关** (#104) — 使用系统原生标题栏时仍可切换左右侧栏。
- **粘贴图片附件 + 带空格路径引用** — 图片粘贴作为附件；路径 chip 保留空格不再截断。
- **终端 Dock 竞态 / 未处理 rejection** — 加固 pending agent 切换与关闭路径，避免
  未捕获 Promise 异常。
- **终端 Dock 按所有者隔离** — Dock 状态按 owner 分桶，项目终端不再跨 agent/会话串台。
- **剪贴板 Document is not focused** — 全栈复制改走 Electron 主进程
  `clipboard.writeText`（preload 暴露），并保留降级路径。
- **本地文件链接可点 + todo 字体跟随** (#103) — 本地文件链接可再次点击；todo 挂件
  遵循界面字体设置。
- **未完成 tool/thinking 回合并入下一条回复** — 保留仅 thinking 的 assistant 回合；
  普通未完成 run 不再错误合并到下一条回答。
- **重发更安全** — 仅截断当前用户回合的后代节点，并拒绝不安全的非末条用户根。
- **Select 取消不再误选第一项** — 取消返回 `value: null`，避免被当成有效选项。
- **Agent `get_state` 超时自动重试** — 启动状态拉取超时后自动重试，避免卡在启动中。
- **Composer 占位符与 prompt 历史** — 清空输入后恢复 placeholder；prompt 历史跨重启
  持久化。
- **手动发版空 tag** — workflow_dispatch 未填 tag 时发布正式 release，而非仅草稿。
- **macOS 测试构建 OOM** — CI mac 构建改用 `build:fast` 并提高 Node 堆上限。
- **package-lock 依赖同步** — 修复合并/工具链漂移后缺失的 lock 条目。

### 🙏 致谢

感谢本版本所有贡献者提交的 PR、Issue 和反馈：

- **@1900EasonJin** — 系统标题栏模式下补充左右侧栏开关 (#104)；宠物状态卡死修复 (#107)
- **@zzq168281-coder** — 本地文件链接可交互 & todo 字体跟随 (#103)
- **@me9rez** — TypeScript 增量编译产物整理 (#97)
- **@weishiair** — 禁用内置扩展时删除用户目录残留文件，修复工具冲突导致 RPC 失败
- **@clancyclaw** — RichInput 多行换行保留

特别感谢 **微时佬友** 提供的 Grok 模型服务，用于我们的社区测试环境 🎉

> 💬 **QQ 反馈交流群：1026218644**

感谢所有给 PiStudio 提建议和反馈 Bug 的用户 🙏

---

## v0.6.6 - 2026-07-24

### 🚀 新功能

- **侧栏品牌区重新设计** — Pi 官方 canvas logo 裁掉棋盘空边，右侧 PiStudio 字标使用 Plantin 衬线字体，
  agent 启动/关闭时播放拼装动画，定格色适配主题墨黑/白。
- **多 Tab 文件编辑器** — 最多 5 个并发编辑器 Tab，弹框/侧栏双模式，Diff 差异对比，Monaco 编辑器
  支持暗色/亮色主题、Markdown 预览、自动保存（Ctrl+S）和脏状态标记。
- **& 会话引用快捷输入** — 键入 & 弹出同项目会话搜索列表，可选择特定消息或引用全部上下文，
  选择持久化，重新打开恢复上次勾选。
- **飞书/Lark 集成** — 双向对话、流式卡片、自动拉群、成员管理，Composer 飞书状态入口。
- **Git 源代码管理（大重构）** — VS Code 风格 3 Tab 面板（变更/历史/比较）、AI 提交摘要生成、
  Git 图形化提交历史（彩色泳道）、Cherry-pick/Revert/Reset/Drop、分支切换、Worktree 支持。
- **Git Push / Pull** — 变更面板标题栏新增 Push 和 Pull 按钮，完整 IPC 链路及错误通知。
- **Prompt 中文提示词精选** — 取代旧 yao-prompts 文件，SQLite 存储 ~4000 条中文提示词，
  支持 20+ 分类筛选、FTS3 全文搜索、分页浏览、一键导入。
- **Skills.sh 社区技能商店** — 切换到 CLI 注册中心，支持安装量排序和安装中动画。
- **HTML 预览改为内置浏览器** — 文件编辑器打开 HTML 默认显示源码，点击预览按钮切换到右侧
  浏览器面板用 webview 渲染，不再受 iframe sandbox 限制。
- **Composer 重新设计（OpenCode 风格）** — 顶部圆框按钮组移至底部操作栏：模式切换/Prompt/
  附件/模型名/思考级别，均可点击。
- **客户端消息队列** — Agent 忙碌时可排队发送（follow-up 或 steer 模式），可撤回已排队
  消息回输入框编辑，可视化排队状态。
- **推荐扩展包改进** — 全部显示复制安装命令按钮，操作按钮横向排列，安装状态按实际变动。
- **技能安装异步执行** — `npx skills install` 改为 `execFile` 异步调用，不再阻塞主进程 UI。
- **内置浏览器面板** — 右侧抽屉浏览，支持标签页、全屏、移动端预设，链接直接在浏览器面板中打开。
- **草稿本（ScratchPad）** — 浮层式草稿本，支持内容预览、勾选映射，使用主题语义颜色。
- **本地快速打包** — `npm run compile-exe` 输出便携 exe，`npm run dist:win` 支持单格式构建。
- **历史会话自动滚动** — 打开历史会话时自动滚动到最新消息。
- **Toast 通知系统** — 自建通知机制替代 `sonner` 依赖，Agent 操作、文件复制、模型切换、Git 操作均有通知。
- **可展开的压缩卡片** — 压缩前消息历史在可折叠区域中查看。
- **WSL 环境支持（实验性）** — 会话扫描、文件操作、路径处理适配 WSL。
- **WSL 环境支持** — 会话扫描、文件操作、路径处理适配 WSL（感谢 @Lopution PR #84）。

### ✨ 交互优化

- **设置页重构** — 全局草稿保存/取消，新 Tab 分类：通用、外观、代理、开发、宠物、存储。
- **字号分区独立配置** — 聊天、代码、侧栏、输入框独立字号，预设字体主题和窗口缩放。
- **文件侧栏增强** — 新建文件/文件夹功能，Git 面板目录树展示，相对路径，抽屉状态按项目持久化。
- **行为选择器移到停止按钮左侧** — 视觉更清晰。
- **Composer 底部栏样式统一** — 所有按钮使用 `composer-bar-btn` 风格（28px 小圆角）。
- **Skills/Prompts 切回本地 Tab 自动刷新** — 安装后立即可见。
- **文件预览** — Markdown 默认渲染预览（可切换源码），HTML 在浏览器面板中预览。
- **Diff 分栏/合并切换** — Modal 模式下可靠工作（key 重建 + keepCurrentModel），
  侧栏模式隐藏按钮（容器太窄无法分栏）。
- **浏览器面板关闭/最大化按钮移至标签栏** — 节省顶部空间。
- **安装按钮旁加复制命令按钮** — 方便手动终端安装。
- **会话大纲 & 快捷操作栏** — 浮动大纲面板，支持跳转到指定消息；快捷操作包括终端、文件抽屉、
  Git、浏览器、草稿本、外部编辑器。
- **匿名会话 Chat 入口** — 项目列表顶部固定 Chat 入口，写入应用用户目录，适合无需绑定代码项目的通用对话。
- **内容宽度限制** — 可拖拽的内容宽度滑块，默认不限宽，往左拖逐渐变窄。
- **Pin 模式** — 将常用 Agent 固定在侧栏顶部。

### 🐛 Bug 修复

- **Monaco CSP 报错** — `loader.config({ monaco })` 移至模块作用域同步初始化。
- **TurnRow "Rendered fewer hooks" 崩溃** — 修复发送消息后白屏。
- **"TextModel got disposed before DiffEditorWidget model got reset"** — 添加
  `keepCurrentOriginalModel` + `keepCurrentModifiedModel` 防止模型销毁竞态。
- **停止按钮在 agent 回答时不可见** — Agent 繁忙时始终显示。
- **匿名 agent 侧栏重复** — 增加 `noSession` 匹配路径。
- **Agent 启动状态卡在 "starting"** — 修复 `setAgents` 覆盖逻辑。
- **同会话重发截断错误** — 修复为只删除最后一条消息的后代条目，而非全部删除之前的内容。
- **Skills.sh 搜索崩溃** — `loadPersisted()` 增加 `Array.isArray` 防御。
- **Prompt 分类无数据** — 修复 slug 和原始名匹配问题。
- **标题栏色差** — 统一 `.window-controls` 背景色。
- **sql.js 打包后加载失败** — 修复 WASM 路径解析。
- **GitService.getStagedDiff maxBuffer 过小** — 从 5KB 提升到 10MB。
- **dev 终端中文乱码** — Windows 下自动 `chcp 65001`。
- **HTML 预览白屏** — 修复 webview 无限刷新和初始加载 ERR_ABORTED。
- **Docs 站构建失败** — VitePress YAML `&` 改为引号包裹。
- **TypeScript CI 构建失败** — 移除重复的 `setAttachedImages` 函数。
- **内置扩展禁用后无法恢复** — 修复禁用后丢失的问题。
- **旧版 pi 兼容** — 优雅降级处理 `--no-approve` 参数。
- **发送消息自动滚动** — 滚动到末尾而非开头。
- **移除思考动画** — 统一使用"正在回应"动画作为默认等待提示。
- **Agent idle 后 stuck 动画** — 添加 fallback idle 检查。
- **多选分享图片内边距** — 添加 padding 避免文字贴边。
- **Ask 弹框交互** — 确认按钮尺寸统一、自定义输入始终可见、弹框打开时隐藏背景卡片、
  过滤 Pi 自带的 ✎ 选项。
- **消息 CPA_DONE 标记清理** — 从消息末尾移除 `CPA_DONE`。
- **用户消息编辑** — 编辑后回填到输入框重新发送。

### 🙏 致谢

感谢所有贡献者提交的 PR、Issue 和反馈：

- **@1900EasonJin** — 飞书集成、记忆管理卡片、Thinking 节流、侧栏卡片化设计、
  草稿本、终端编码修复 (#80, #74, #60, #44, #42, #35, #34)
- **@frostime** — 会话信息同步、自定义字体/缩放、模型选择器自动滚动、
  最大推理等级、RPC 扩展生命周期 (#58, #56, #53, #52, #50)
- **@me9rez** — 依赖清理、SkillManager 软连接扫描 (#86, #69)
- **@bfzha** — VS Code 风格 Git 面板及复杂工作流 (#68)
- **@Lopution** — 跨桌面 WSL 路径处理 (#84)
- **@buaassp** — 隐藏内部 pi-subagent 子会话 (#57)
- **@magic2066** — Codex 子代理导入修复、Linux 开发/宠物修复 (#40, #41)
- **@pangolinknight** — 流式消息节流、工具结果截断、白屏修复 (#33)

特别感谢 **微时佬友** 提供的 Grok 模型服务，用于我们的社区测试环境 🎉

---


## v0.6.5 - 2026-07-13

### 🚀 新功能

- **Prompt 模板系统（大块）**
  - `PromptManager` 完整 CRUD 和 IPC 桥，管理 `~/.pi/agent/prompts/` 模板
  - `PromptsTab` 配置页（Monaco 编辑器创建/编辑/预览/删除）
  - 输入框 `/` picker 选择模板，`$N` 占位符自动提示
  - 项目级 prompt（ProjectResourcesModal 内创建/编辑/删除）
  - 内置模板：review、test、fix、refactor、doc、explain、commit、pi-system、skill-discipline
  - 发送时剥离 frontmatter，附带 `description` 元数据
  - prompt/skill 名称支持 Unicode（中文、日文等）
- **Prompt/Skill 商店集成**
  - `prompts.chat` 商店搜索/预览/导入，变量提示语法自动转换
  - 集成 Yao Open Prompts：121 个中文 Prompt，覆盖 9 个分类，支持分类筛选/搜索/预览
  - 新增 Skill Store 标签页
- **Git Worktree 工作区管理**
  - `WorktreeService`：检测 git worktree，IPC 创建和删除
  - 分支列表 + 创建对话框 + 删除按钮，按 worktree 分组展示会话
  - 主工作区 header 点击加载父项目会话
  - 启动时自动刷新 worktree
- **消息多选 & 分享**
  - 复选框多选模式，浮动操作栏支持文本/markdown/图片复制
  - 图片复制走 `toBlob()` 修复 CSP 问题
  - 复制成功脉冲动画 + toast 反馈
- **内置浏览器预览**
  - 右侧抽屉新增浏览器面板，支持多标签、地址栏、刷新/主页/前进/后退
  - 支持全屏模式和 PC/手机/平板视口预设，便于不离开 PiStudio 快速查看网页
  - 不支持的外部协议自动回退到系统默认浏览器打开
- **会话管理器弹框**
  - 项目右键菜单打开，列表展示全部会话，支持多选删除
  - 每条会话可重命名、导出、删除，来源筛选（Pi/Codex/Claude/OpenCode）
  - 统一 1300×850 弹框尺寸 + 背板点击关闭
- **外部编辑器集成增强**
  - 项目右键菜单新增「打开方式」→ 选择编辑器直接打开项目目录
  - 编辑器弹窗定位改为 left/top + 视口夹紧，支持左侧项目栏触发
- **Prompt 配置增强**
  - 模板选择器显示 description + 变量提示，选择更直观
  - 模板展开用 `\n\n` 分隔命令与用户输入
  - 会话文件摘要从聊天区移到输入框上方（可折叠）
  - 全局和项目级 prompt 重命名支持
- **模型配置**
  - 新增 `xhigh` 推理级别

### ✨ 界面优化

- **抽取公共 MonacoEditor 组件**：CSP 兼容本地 worker，暗色主题统一，ConfigModal/ProjectResourcesModal/PromptsTab 共用
- **思考卡片视觉刷新**：去掉边框、显示耗时、箭头放标题后、hover 浅色
- **工具卡片**：去掉边框和背景色，与思考卡片风格一致，详情用三级文本色
- **回答字号**：提升至 15px，行高 1.68
- **Turn row 间距**：块间间距从 8px 增大到 12px
- **Extension Widget**：重新设计为可折叠卡片 + 关闭（X）按钮
- **统一弹框尺寸**：全屏弹框统一 1300×850 + `min(vw,vh) - 48px` + 背板点击关闭
- **统一图标按钮**：SkillsTab/ExtensionsTab/ProjectResourcesModal 文字按钮 → lucide 图标按钮，hover 显示标题
  - 启用/禁用切换：ToggleRight(绿色高亮)/ToggleLeft(默认)
- **模型选择 UI 精简**：288 行 → 124 行
- **Enter 换行**：浏览器原生处理，不再手动插入 `<br>`
- **中文 prompt 名称**：chip 正则支持 `\p{L}` Unicode，移除 `[a-zA-Z]` 限制

### 🔧 性能优化

- **会话打开优化**：`get_state` + `get_messages` 并行发起
- **loadMessages**：`get_messages` + `get_entries` 通过 Promise.all 并行
- **初始会话加载**：跳过 `get_entries`（延迟到编辑/删除时）
- **IPC 传输裁剪**：剥离 tool ChatMessage meta 中的 `originalContent`
- **历史消息计数**：按对话轮次（20 轮）而非原始消息数
- **移除 `repairAssistantUsage`**：导入器已补齐，无需每次打开检查
- **loadMessages 重试**：仅在失败时重试，不再无条件二次调用
- **清理全部 `[perf]` 调试日志和未使用的计时代码**

### 🐛 Bug 修复

- **Windows 启动崩溃修复**：全局禁用 Chromium 沙箱（`--no-sandbox`），解决 `0x80000003` 断点异常
- **pi 自动压缩后进程重启处理**：
  - 新增 `compactingAgents`/`userInitiatedStop`/`autoRestartAttempted` 跟踪集合
  - 进程退出处理器三层判断：用户停止 / 压缩中 / 干净退出自动重连
  - `reattachProcess()` 保留 agentId + 消息，替换 PiProcess + RPC 客户端
  - 手动压缩 RPC 失败后自动 `reattachProcess()`（压缩已写到会话文件）
  - Stop/stopAll 标记用户主动停止，跳过自动重连
- **onCompact 事件污染**：MouseEvent 传入 IPC 导致结构化克隆失败，改为 `() => compactAgent()`
- **Extension RPC 生命周期**：
  - 扩展命令在会话输出后清空（而非之前）
  - 非对话框 UI 请求渲染为卡片，不弹出气泡
  - 扩展 UI 请求生命周期：agent_end 时清空 pending
- **消息渲染修复**：
  - TurnRow 按 `run.items` 原始时序渲染，恢复思考/工具/回答交错显示
  - `showThinking` 动态从 pi agent 读取，切换 agent 时生效
  - 兼容 Anthropic-compatible 服务把同一段回答拆成多个 `content[].text` 的输出，拼接时不再额外插入换行，修复回答呈“竖排”显示
  - `<button>` 嵌套修复：ExtensionWidgetCard 关闭用 `<span role=button>`
- **Worktree**：细化项目处理和 worktree 下会话加载
- **分享和 Widget UI**：视觉优化和布局修正
- **Prompt frontmatter**：`description` 不再重复写入正文
- **内置 prompt 描述翻译**：根据语言自动切换中/英文

### 🛠 重构

- 将 AppParts.tsx 中非组件导出抽取到 AppUtils.ts（修复 Vite Fast Refresh 警告）
- RPC 扩展命令空闲检查逻辑明确化

## v0.6.4 - 2026-07-05

### 🚀 新功能

- **计划模式 (Plan Mode)**：Composer 工具栏新增模式切换器，支持计划模式与
  普通模式相互切换。计划模式下 Agent 先生成计划再执行，执行结果可逐条确认；
  取消计划后返回选单而非空停。
- **ask_question 扩展全面增强**：
  - 支持批量提问，一次发送多个问题并返回结构化结果
  - 选项列表支持选中高亮和确认反馈
  - 折叠时 subtitle 显示提问内容
  - 结果持久化到 `meta._askCard`，重启历史会话后仍能正确渲染
  - 增强 promptGuidelines，使其更符合规则性指令
- **消息编辑/删除功能**：
  - AI 回答支持复制、编辑和删除操作
  - 用户消息支持编辑/删除，编辑后回填到输入框重新发送
  - 修复删除失败/闪回和编辑不同步问题
  - 新增计划模式取消功能
- **草稿本 (ScratchPad) 浮层**：全新的草稿本浮层，支持内容预览、勾选映射、
  入口迁移和右对齐动画，颜色统一使用主题语义 token。
- **pi-deck-todo 内置扩展**：新增 todo 列表内置扩展，支持任务管理；
  widget 渲染按 widget key 独立成块，长文本支持截断与滚动。
- **内容行宽限制设置**：新增可拖拽的内容宽度限制滑块，默认不限宽，
  往左拖逐渐变窄，最小限制 800 px。
- **Thinking 块重构与状态指示器**：
  - 思考内容改为 ThinkingBlock 卡片展示，AssistantText 还原为纯文本
  - 思考块按 `<thinking>` 标签原位交替渲染，不合并也不挪到最后；
    保留 content 数组中 thinking 块的原始交替顺序
  - ThinkingBlock 默认展开，推理过程在思考结束后保持可见，可手动折叠
  - ThinkingBlock trigger 添加内容预览子标题，字号与 tool-card 一致
  - 工具栏"运行中"圆点改为消息列表底部三点脉动动画，支持"思考中"、
    "正在执行 {tool}"和等待三种状态，模型开始响应后自动隐藏
  - 优化等待指示器间距，三点动画与上一条消息保持 16 px 间距
  - 扁平时间线渲染 + 统一消息间距 + 内联思考段
- **扩展管理增强**：
  - 支持禁用/启用内置扩展，禁用按钮带动画优化
  - 项目级技能/扩展管理，区分全局与项目级配置
  - 修复 extension_ui_request 字段读取路径（pi RPC 放顶层而非 params）
  - getToolKind 区分 MCP-direct 与含下划线的扩展工具
- **信任确认系统**：信任确认改由桌面端拦截，不信任仍可打开项目；
  有 Agent 运行时禁止删除项目。
- **DiagnosticMessageCard**：新增错误/系统消息卡片，支持语气区分样式。
- **设置页增强**：
  - defaultProvider/defaultModel 下拉支持联动和自动发现模型
  - 新增 enabledModels 多选 UI 和模型收藏功能，收藏模型固定在顶部
  - 使用 lucide Star 图标替代 Unicode ★
  - 重启 Agent 不再自动切换选中项，设置页去掉易误配的重试项
- **会话体验增强**：
  - 会话压缩事件显示 + 会话文件路径可点击打开
  - 项目行添加一键新建 Agent 按钮
  - 外部编辑器入口 + 会话定位默认显示 + 去除入口阴影
  - 无对话时会话定位变灰且 hover 不消失
- **飞书助手增强**：
  - 优化飞书模型切换卡片，修复表格富文本渲染
  - 修复飞书文件发送误触发和重复发送问题
  - 精简飞书助手代码

### ✨ 界面优化

- **思考卡片呼吸动画**：流式思考卡片添加边框脉冲辉光和背景微呼吸，即使思考
  文本卡住也能感知系统活跃。
- **思考卡片背景色**：与工具调用 running 状态一致的微色背景。
- **Web 搜索卡片二级标题**：web_search / fetch_content 卡片折叠后显示查询词
  或 URL 作为二级标题。
- **内容宽度范围调整**：滑块最小值从 50 提升至 800 px，避免编辑区缩到极窄。
- **等待指示器间距**：三点动画与上一条消息保持 16 px 间距。
- **Composer 优化**：默认高度降低 25px + 发送后强制恢复默认高度；
  composer 下移 10px，增加底部留白。
- **终端开合动画**：改为平滑动画，从输入框下方滑入，不再突兀弹出。
- **右侧抽屉动画优化**：网格布局过渡动画更流畅，回退到 0.18s ease 版本。
- **草稿本体验优化**：预览勾选、入口迁移、动画、右对齐，文件列表默认折叠。
- **聊天区背景统一**：浅色模式统一为 `#fcfcfc`，暖白模式为 `#fbfaf7`。
- **工具卡片修复**：解析 JSON 字符串参数、移除耗时阈值、摘要放时间后。
  - 恢复工具调用耗时显示
- **斜线命令说明改为中文**，匹配下拉选单显示。
- **Branch-dropdown 居中定位** + 统一新建 Agent 背景色。
- **ConfigModal**：User-Agent 字段布局错位修复，兼容选项补充说明。
- **兼容性设置**：未勾选时显式写入 false 值，简化 desc 文案。

### 🐛 Bug 修复

- **暗色模式白色背景**：修复 `.chat-pane`、`.composer`、`.composer-box` 及
  loading 遮罩的硬编码 `#fcfcfc`，暗色模式下正确适配面板背景色。
- **RichInput 换行修复**：修复 contentEditable 中多行粘贴时换行丢失问题。
- **消息渲染修复**：
  - 提高全局消息间距规则特异性，覆盖组件自身 margin
  - 移除 .thinking-card.streaming 的 margin-top:0，恢复全局 16px 间距
  - extension-widget-stack 和 composer-footer 兼容内容宽度限制
  - AssistantText 支持 message.thinking 独立字段回退显示
  - 按时间顺序渲染 thinking/tool, 修复自动滚动和缓存命中率
- **plan 模式修复**：跨会话锁死、会话内无法退出、破坏斜线命令等问题。
  - 取消计划后回到选单而非空停
  - 弹框选项改为更自解释的描述
- **ask_question 修复**：三处错误修复，扩展集成恢复（被 scratchpad 覆盖）。
- **代码编辑/删除修复**：角色检测错误、reload 状态不同步、删除失败/闪回。
- **Linux Wayland 修复**：桌面宠物拖拽修复和 dev 启动优化。
- **飞书修复**：表格富文本渲染、文件发送误触发和重复发送、会话文件发送。
- **Codex 子代理会话导入展示修复**，分组到父会话下。
- **Pending Agent 不加载终端**，已关闭终端静默忽略 resize。
- **重新生成 package-lock.json** 修复 npm ci 失败。
- **恢复 ask_question 扩展集成**（被 scratchpad 改动覆盖）。

## v0.6.3 - 2026-06-28

### 🚀 新功能

- **桌面宠物系统 (MVP-2)**：全局透明悬浮窗，Canvas 动画引擎，
  idle/巡游/Review/逗弄交互，通知气泡，Linux/Wayland 降级圆角小窗
- **内置宠物**：clawd / cache-capy / duo / octohack / fangjia 五种，
  选择器新增 Canvas 动画预览
- **ContentEditable Chip 输入系统 (#24)**：`@路径` 和 `/命令` 渲染为可视化
  交互式 chip，文件 chip 可点击打开；光标感知建议触发；IME 中文输入保护
- **居中模态对话框**：设置、配置、反馈改为居中覆盖模态框，backdrop 点击关闭
- **消息渲染增强**：新增浅色背景主题选项
- **批量选择模型**：模型获取后支持批量选择多个
- **OpenCode 会话导入**：支持导入 OpenCode 本地会话
- **会话来源徽章**：Codex/Claude/OpenCode 来源标识，支持按项目过滤
- **RPC 超时提升**：最小超时提升至 600 秒
- **Pi/扩展更新 UI**：信任管理标签页、平台筛选

### ✨ 界面优化

- 会话统计：SessionStatus 栏新增 token/cache chip
- 模型选择器：搜索结果分组支持折叠/展开
- 扩展加载动画：列表加载时显示动画避免空白歧义
- 滚动到底按钮：基于 ResizeObserver 自动滚动

### 🐛 Bug 修复

- **macOS 终端修复**：node-pty spawn-helper 权限及路径替换错误
- **宠物 IPC 时序**：修复宠物开关丢失、启动时错误宠物闪现
- **终端层级穿透**：TerminalDock z-index 调整，打开模态时隐藏终端
- **RichInput 换行丢失**：修复 contentEditable 中 `\n` 被 `<br>` 吞掉
- **Compact 斜线命令**：修复 `/compact` 处理逻辑
- **拖拽切 idle**：巡游拖拽后即时切换 idle
- **扩展状态**：修复安装状态检测和输入引用识别
- **会话统计 & TS 错误**：修复 4 个类型错误、过滤持久化
- **构建脚本恢复**：恢复 4 个被误删的工具脚本
- **历史会话**：优化加载速度、滚动到底体验、自动写入 trust.json
- **Agent 状态翻译**：补充 app.statusError 中英文翻译
- **右键菜单重复**：修复 RPC 日志开关显示重复文本

### 🔧 性能优化

- **流式卡顿**：AssistantText 包 memo，mermaid 动态 import()
- **宠物代码精简**：重构后代码缩减 41%（10 文件，−1096 行）

### 📦 工程

- 恢复 package.json/package-lock.json 至上游版本
- 添加 @1900EasonJin 到贡献者列表

### 📖 文档

- 新增 pet-only PR 描述文档
- README 和文档站点添加 QQ 交流群信息

### 🔁 CI

- macOS x64 runner 从 macos-13 迁移到 macos-latest

### 🤝 贡献者

感谢 @ayuayue、@1900EasonJin、@zx3022448 对本版本的贡献！

## v0.6.2 - 2026-06-22

### 🚀 新功能

- **项目子项统一列表**：Agent 和历史会话在同一个时间轴混合排列，每个项目默认最多显示 5 项
- **外部编辑器管理**：设置中新增编辑器配置页，支持检测、启用/禁用和配置编辑器
- **Windows 注册表检测编辑器**：增强浏览器检测逻辑，通过注册表发现已安装的编辑器
- **文件操作增强**：Monaco 差异编辑器、Git 工作区变更面板、编辑器打开入口
- **飞书流式卡片 v4**：活动轨迹实时可见、闪电确认、并行启动 Session Mirror
- **飞书桥远程控制**：通过飞书机器人远程控制 Agent
- **飞书维护手册**：新增架构/实现原理/远程控制维护指南

### ✨ 界面优化

- **顶部操作按钮统一**：“新会话”“文件”“终端”高度、padding、字号、基线一致
- **日志页优化**：新增日志级别筛选和时间范围筛选
- **左下角增加官网入口**：点击打开 PiStudio 官网

### 🐛 Bug 修复

- **历史会话重复显示 / 激活**：修复路径大小写/分隔符导致的历史会话和 Agent 同时显示
- **历史会话打开后消息空白**：移除预热池进程复用，每个 Agent 独立启动
- **点击历史会话顺序被提前**：仅打开不发送消息不再影响排序
- **快速重复点击创建多个 Agent**：主进程增加 sessionPath 级别创建去重锁
- **飞书流式卡片结果不显示**：修复卡片消息渲染失败问题

## v0.6.1 - 2026-06-16

### 🚀 新功能

- **配置管理批量删除**：Provider 和 Auth 支持批量选择删除，勾选后一键清除
- **配置管理复制**：Provider 和 Auth 支持一键复制配置，快速创建变体
- **删除确认对话框**：删除 Provider、Model、Auth 时弹出确认提示，防止误删
- **Auth 供应商选择器**：内置 29 个支持的供应商（Anthropic、OpenAI、DeepSeek 等），带环境变量和获取链接
- **Provider 配置指南**：内置 API 类型说明、兼容性配置、排错指南，含官方文档链接
- **Auth 配置指南**：三步引导用户完成认证配置
- **模型选择器分组折叠**：按供应商分组，支持折叠展开，搜索时自动展开
- **API 类型下拉优化**：新增标签和详细描述，帮助用户正确选择 API 类型
- **User-Agent 预设扩展**：新增 claude-cli、claude-code、Kilo-Code 等预设选项

### ✨ 优化

- **会话区域 header 紧凑化**：标题与路径同行，状态信息在第二行，模型/思考/cache 芯片带颜色点缀
- **模型选择器树形展示**：缩进 + 左边框 + 分组标题，视觉层次清晰
- **滚动条显示**：会话区域和模型选择器显示细滚动条
- **新会话排序**：新建会话自动排到列表最前面
- **按钮文案**："启动" → "新会话"，"+ Provider" → "+ 添加供应商"
- **表单标签左对齐**：配置表单 label 统一样式
- **卡片列表高度缩小**：配置管理卡片更紧凑
- **获取模型按钮移位**：从表单顶部移至模型列表 header
- **高级字段提示块样式优化**：从蓝底大字改为灰色左侧边框
- **自定义供应商输入**：明确提示用于添加预设列表之外的供应商
- **复选框间距优化**：checkbox 与文字间距加大到 10px
- **批量删除按钮红色**：入口按钮和删除选中按钮使用红底白字

### 🐛 修复

- 修复 Agent 列表状态文字换行问题（添加 nowrap）
- 修复切换 Agent 后之前激活的 Agent 状态不显示
- 修复 anthropic-messages 连接测试 max_tokens=1 导致误报 404
- 修复模型选择器横向滚动条
- 修复批量模式下点击 checkbox 触发展开
- 修复删除确认对话框按钮文字被背景遮挡

### 🌐 多语言

- 统一术语：Provider → 供应商，Auth → 认证
- 新增 path、ctx、cache 等 UI 文案翻译
- 思考等级显示支持多语言（关闭/低/中/高等）
- 新增 40+ 个翻译键覆盖所有新增功能

## v0.6.0 - 2026-06-14

### 新增
- Claude 会话导入：项目右键菜单可扫描并导入本地 Claude JSONL 会话，转换为 PiStudio 历史会话。
- 输入框命令历史：在首行/末行使用方向键可快速复用之前发送过的提示词。
- 性能测试脚本和渲染辅助能力，用于验证长会话渲染优化效果。

### 优化
- **会话流程展示**：思考、工具调用和回答片段现在以更紧凑的活动流展示，状态、时间对齐、换行和详情复制更清晰。
- **历史会话性能**：显著减少打开历史会话时的输入延迟（平均性能提升 90.3%）。
  - 消息更新优化：添加引用相等检查，跳过不必要的状态更新
  - 建议项计算优化：仅在建议框打开时才计算建议项
  - 文件修改摘要优化：仅在消息数量变化时重新计算
  - 会话轮廓优化：优化依赖项，减少重计算频率
- **工具调用状态**：bash 命令退出码现在作为命令业务结果展示，不再直接标记为 RPC 工具调用失败。
- **启动体验**：应用启动后窗口自动最大化，提供更大的工作空间。
- **输入框体验**：输入框默认高度从 132px 增加到 160px，更适合多行输入和代码片段编写。
- **输入响应性**：输入框打字更流畅，特别是在长对话会话中。

### 修复
- Windows 绿色版设置在重启后正确保留。
- 系统托盘行为更稳定。

## v0.5.0 - 2026-06-14

### 新增
- 局域网 Web 服务：设置页可启动本机 HTTP 服务，局域网内其他设备可通过电脑 IP 和端口访问 PiStudio。
- pi Extension 管理：配置管理中新增扩展管理能力，与 Models、Auth、Settings、源文件和 Skills 放在同一配置工作流中。
- Git 新建分支：分支选择器支持基于当前分支直接创建并切换到新分支。
- 项目右键定位：项目目录可直接从右键菜单在系统文件管理器中定位。
- VitePress 文档站与完整 UI 设计审计文档，记录当前桌面工作台架构和设计系统演进方向。

### 优化
- 桌面工作台大幅刷新：项目侧栏、对话区、抽屉、输入区、分隔条、右键菜单和弹层表面已迁移到统一的语义 token 体系，覆盖字体、颜色、间距、圆角、焦点和动效。
- 暗色模式覆盖范围显著扩大，主工作区、设置、配置管理、反馈、RPC 日志、Codex 导入、图片预览、消息流、工具调用、终端 Dock 和确认弹窗在明暗主题下更一致。
- 设置、配置管理和反馈页已改为更稳定的全屏页面式弹层，并适配自定义 Electron 标题栏，避免与左上 PiStudio 标识重叠。
- 侧栏工作流更清晰：项目下直接展示最近历史会话，左键打开或复用会话，右键仅保留管理操作，并移除 Agent 行右侧关闭按钮以减少误触。
- Agent 与历史会话右键菜单聚焦管理操作；历史会话可在侧栏右键中重命名、复制、导出、查看 RPC 日志或删除。
- 设置页下拉框改为 PiStudio 自绘 Select，不再使用系统原生下拉弹窗。
- Header 操作区按分支上下文、会话操作和面板切换分组；模型/状态 chip 留白增加，不再贴近底部分隔线。
- 公共 UI 组件进一步沉淀，按钮、图标按钮、关闭按钮、文本输入和下拉控件在设置、配置、反馈、更新、环境检测和导入弹窗中更统一。
- PiStudio 品牌字标、字体角色、Logo 处理、图片预览遮罩、选择器面板和终端字体进一步统一，整体桌面感更强。
- 中英文与伪翻译覆盖显著扩大，工作区、配置、设置、窗口控制、反馈、更新、RPC 日志、模型/思考选择器和低频 toast 文案更完整。
- 终端 Pi Soft 主题会跟随暗色模式使用专门的 xterm 配色。

### 修复
- 输入框编辑时方向键不再误触发历史导航。
- Windows pi shim 启动链路保持预期 Node 运行时，减少环境不一致导致的启动问题。
- 配置管理对白屏和复杂配置结构的异常恢复更稳。
- Codex 导入会话会保留原始时间戳作为创建和更新时间，导入后的会话排序更稳定。
- 自定义标题栏布局下，设置和配置页面标题不再与 PiStudio 字标重叠。

## v0.4.17 - 2026-06-11

### 新增
- 全局 Skill 管理：配置弹窗新增独立 Skills 页面，可列出 `~/.pi/agent/skills` 和 `~/.agents/skills` 中的全局 Skill。
- Skill 操作：支持创建 Skill 模板、启用/禁用模型自动调用、通过应用内确认弹窗删除 Skill，以及打开 Skill 所在目录。
- pi 路径手动兜底：自动检测失败时可输入自定义 pi 路径，设置页会常驻显示当前实际使用的 pi 路径。

### 修复
- Windows pi 命令校验现在支持包含空格的 `.cmd` shim 路径，避免 Node 二次转义破坏 `cmd.exe /c` 命令行。
- 手动 pi 路径校验会在保存前归一化带引号路径、双反斜杠路径和无扩展名路径。
- Windows 检测链路不再依赖 PowerShell `pi.ps1` shim，减少引号、编码和执行策略导致的失败。

### 优化
- Skill 列表改为与会话历史列表一致的紧凑卡片风格。
- pi 环境检测失败会在设置页内显示详细原因，启动检测弹框仍保留。

## v0.4.16 - 2026-06-11

### 新增
- 匿名使用统计：打包应用现在每天最多发送一次 `app_heartbeat`，用于了解版本分布、平台兼容性和活跃安装数量。
- 隐私控制：设置页新增匿名使用统计开关，可随时关闭。

### 优化
- 隐私文档现在说明心跳会采集哪些最小字段、不会采集哪些内容，以及第三方统计服务会接收请求元数据。
- Telemetry 测试覆盖禁用、未打包、缺少项目 key、每日限频和 PostHog 用户属性同步等边界。

## v0.4.15 - 2026-06-09

### 新增
- 内置 Chat 对话区：项目列表顶部新增固定 Chat 入口，适合不需要绑定代码项目的通用对话。
- 项目拖动排序：普通项目行现在可拖动调整顺序，重启后仍会保留自定义排序。

### 修复
- 终端 scrollback 恢复：切换到其他 Agent 后再回来，终端输出和滚动条状态会正确恢复。
- Agent 启动焦点：新建 Agent 仍在启动时，如果用户切到其他 Agent，启动完成后不会再抢回当前会话。
- 输入草稿隔离：每个 Agent 现在只保留自己的未发送文本和图片附件，不再共用一个全局输入框草稿。
- Provider 连接测试改用更小的探测请求，并补充更清晰的超时说明，减少 reasoning 模型首包慢或上游排队导致的误判。

### 优化
- 应用图标、启动页 logo 和内置 Chat 入口改用新的 `#14b814` 品牌绿，同时普通项目头像保持更克制的中性色。

## v0.4.14 - 2026-06-09

### 优化
- 发版包体积：构建期依赖和仅用于渲染进程的前端库不再作为生产依赖打入应用包，降低 Windows、macOS、Linux 各平台安装包和压缩包体积。

## v0.4.13 - 2026-06-09

### 修复
- Windows pi 路径处理：环境检测和 RPC Agent 启动现在都能处理包含空格的 npm shim 路径。
- 长回答宽度限制：长文本回答、历史会话、思考块、代码块和表格不再撑出会话区域。

## v0.4.12 - 2026-06-09

### 新增
- 运行中消息投递模式：Agent 流式执行时可选择 `steer` 加入当前回合、或选择 `followUp` 排队到 Agent 停止后发送。
- 用户消息投递标记：运行中发送的消息会显示“下次调用前”或“结束后排队”，便于区分消息何时被 Agent 消费。

### 优化
- 短用户消息气泡现在会按实际内容宽度收缩，即使带有投递标记也不会被时间栏撑长。

## v0.4.11 - 2026-06-08

### 新增
- 项目历史快捷入口：每个项目行右侧新增固定历史按钮，无需再依赖右键菜单即可打开历史会话。
- 回答级修改文件摘要：Agent 每轮回答完成后，在对应回答下方展示本轮修改文件名和修改行数的紧凑列表；Files 面板继续保留本次会话总览。
- 应用内检查更新：定时检查最新 GitHub Release，发现新版本后展示发布日志和浏览器下载入口。
- 更新失败指引：手动检查更新失败时说明 GitHub 连接问题，提示可配置桌面端代理，并提供 Release 页面兜底入口。

### 修复
- Agent 终端隔离：切换项目或 Agent 时，不会再继承其他 Agent 的终端展开状态。
- 终端初始化竞态：打开终端时不再因为重复初始化自动创建多个终端 tab。
- macOS 应用图标打包：发版产物现在会生成真实 `.icns` 文件，改善 Dock 图标显示效果。
- 输入框换行、拖拽与窗口边界：长文本在输入框中的换行和滚动更稳定，输入框拖到最大高度后也可再次缩小，同时窗口不再缩小到会破坏布局的范围。
- 检查更新提示清理：手动检查更新的结果提示会自动消失，不再固定停留在窗口底部。
- 项目历史刷新反馈：历史会话弹窗点击刷新时会显示 loading 反馈。

### 优化
- 模型默认值：新增模型时默认写入 `contextWindow=1000000`、`maxTokens=128000`，并默认开启 reasoning。

## v0.4.10 - 2026-06-08

### 新增
- 项目历史快捷入口：每个项目行右侧新增固定历史按钮，无需再依赖右键菜单即可打开历史会话。

### 修复
- Agent 终端隔离：切换项目或 Agent 时，不会再继承其他 Agent 的终端展开状态。
- 终端初始化竞态：打开终端时不再因为重复初始化自动创建多个终端 tab。
- macOS 应用图标打包：发版产物现在会生成真实 `.icns` 文件，改善 Dock 图标显示效果。
- 输入框换行与窗口边界：长文本在输入框中的换行和滚动更稳定，同时窗口不再缩小到会破坏布局的范围。

### 优化
- 模型默认值：新增模型时默认写入 `contextWindow=1000000`、`maxTokens=128000`，并默认开启 reasoning。

## v0.4.9 - 2026-06-08

### 新增
- 项目历史会话弹框：可从项目右键菜单打开历史会话，并在列表中直接重命名会话。
- 终端选区复制：选中终端文本后右键即可复制，并显示轻量“已复制”提示。

### 修复
- Codex 导入会话会补齐 pi 兼容的 assistant usage 元数据，修复继续对话时报 `totalTokens` 读取错误的问题。

### 优化
- Codex 会话导入默认不再自动勾选任何条目，避免未确认时批量覆盖或导入。
- 历史会话列表改为更紧凑的 Codex 风格布局，重命名操作更轻量。

## v0.4.8 - 2026-06-07

### 新增
- pi agent 代理设置：为新启动的 pi agent 子进程注入代理环境变量，并支持 OpenAI API 连通性检测。
- 桌面端代理设置：模型拉取和 Provider 连接测试可通过 Electron 桌面端网络代理发起请求。

### 优化
- 设置弹窗重构为基础设置、代理设置、开发设置三个页签，并补充更明确的保存反馈。
- 新增 Provider 时默认不再写入 User-Agent header，留空时保持 pi / SDK 运行时默认值。

## v0.4.7 - 2026-06-07

### 新增
- 内嵌终端 Dock：可在聊天时间线和输入框之间打开当前 Agent 绑定的终端，无需离开会话。
- 终端多 tab：支持新建、切换、关闭单个 tab，也可通过应用内确认弹层关闭全部 tab。
- 终端主题：支持 Pi Soft、Solarized Light、Solarized Dark、One Dark、Monokai 等经典主题。

### 优化
- 将大型配置弹窗拆分为独立页签和共享工具，后续维护 Provider、Auth、Settings 和原始 JSON 编辑更稳。
- 将主界面的展示组件从 `App.tsx` 拆出，降低主入口复杂度，为后续面板和终端迭代做准备。
- Windows 打包改用 `node-pty` 预构建产物，不再强制 native rebuild，避免 electron-builder 依赖 Visual Studio Spectre 库。

## v0.4.6 - 2026-06-07

### 新增
- Provider 模型发现：可直接从已配置的供应商接口拉取可用模型列表。
- Provider 连接测试：启动 agent 前可用最小请求验证 Base URL、API Key、模型 ID、自定义 headers、延迟和 token 用量。
- Provider 管理增强：Models 页支持重命名 provider，并可视化配置请求 headers / User-Agent。

### 优化
- API 类型兼容：移除非 pi 官方的 `openai-chat-completions` 预设，将历史别名迁移为 `openai-completions`，并让 provider 测试与 pi 官方 Chat Completions 名称保持一致。
- 斜线命令和文件建议支持键盘选择，输入框操作更顺滑。
- 增加 OpenAI Responses 兼容处理，包括为会校验客户端 headers 的 provider 模拟 SDK User-Agent。
- 同步更新配置预览 mock 和 IPC 契约，覆盖 provider 模型拉取与测试流程。

## v0.4.5 - 2026-06-05

### 新增
- 配置管理支持导出/导入功能：可将 models.json、auth.json、settings.json
  三个配置文件打包为 JSON 文件备份，方便恢复和迁移。
- Provider 配置新增兼容性设置：可视化编辑 supportsDeveloperRole 和
  supportsReasoningEffort 选项，无需手动修改 JSON。
- 输入框图片支持点击放大预览：附加图片后点击缩略图可查看原图。
- 文件抽屉顶部显示当前会话 agent 修改过的文件列表。
- 修改文件列表支持右键菜单：打开文件、在文件夹中显示、引用到输入框。
- 会话结束后在状态栏显示总耗时（如 3.2s / 1m23s）。
- Reload/Restart 按钮增加 loading 文字和禁用状态，避免重复点击。

### 修复
- 错误检测逻辑优化：避免将正常工具输出误判为错误消息（如 "Successfully
  replaced" 等成功提示不再显示为红色错误）。
- 输入框图片预览区域与 textarea 重叠问题：调整 grid 布局，图片预览
  独占一行，不再遮挡输入框。
- Agent 异常结束时（API 错误等）将错误提示写入会话，避免空白响应。
- 修正 agent_end 错误提取逻辑：遍历 messages 数组查找异常消息，不再依赖固定位置。
- 增大修改文件列表字体和颜色对比度，提升可读性。
- Git 分支选择器仅显示本地分支，去掉远程分支。

### 优化
- 配置弹窗 UI 优化：宽度增至 900px，导出/导入按钮与保存按钮风格统一，
  Provider 展开区域增加间距，删除按钮图标统一。
- 关闭按钮颜色加深，提高可见性。
- 移除 Reload 按钮（/reload 无法通过 RPC prompt 正确执行），统一使用 Restart 按钮。

## v0.4.4 - 2026-06-05

### 新增
- 输入框上下键切换历史消息：在 composer 中按上/下方向键，可回溯最近发送过的
  消息（类似 CLI 体验）。
- 用户消息编辑按钮：点击可将消息文本回填到输入框，编辑后重新发送。
- Models 页签 API 类型改为自定义下拉选择：预设 openai-completions、
  openai-chat-completions、openai-responses、anthropic、google-generative-ai，
  同时保留自定义输入兼容未知类型。

### 优化
- 配置管理弹窗 UI 整体美化：Models/Auth 展开卡片、表单控件、模型列表面板等
  与主界面风格统一，圆角、浅边框、聚焦绿色描边。
- Agent 启动/重启不再阻塞其他 agent：去掉全局 agentLoading 遮罩，改为按
  当前活跃 agent 的 status 判断，切换项目不再等待。
- 配置保存后不再自动 reload，用户可通过 Restart 按钮手动重载。
- Agent 响应中禁止切换 model 和思考级别，按钮自动禁用避免中途误操作。
- 工具调用状态修复：改为检查最后一条工具消息的状态，工具组完成后不再显示
  "工具调用中"。
- 思考气泡位置恢复至消息列表底部，流式输出时按时间顺序自然堆叠。

## v0.4.3 - 2026-06-04

### 新增
- 思考过程实时显示：模型推理时展示可折叠的思考内容，避免用户感觉“卡住”。
  思考内容持久化到消息中，当前会话和历史会话均可查看。
- RPC 日志面板：右键 agent 标签页打开，展示详细的请求/响应/事件流，
  点击可展开查看完整 JSON 数据。
- 设置页新增 DevTools 开关按钮，方便调试。

### 优化
- 设置弹窗宽度从 420px 增加到 640px，提升可读性。
- 清理思考内容中的 ANSI 转义码（终端颜色序列如 `\x1b[38;2;...m`）。

## v0.4.2 - 2026-06-04

### 新增
- Agent 忙碌时消息自动排队：正在输出时发送消息会进入本地队列，
  agent 空闲后自动逐条发送（带 steer 语义，对齐 pi CLI 行为）。
- 排队消息支持单条取消，点击气泡上的"取消排队"即可移除。
- 排队消息 UI：半透明虚线气泡 + 旋转排队图标 + 发送按钮"排队发送"脉冲动效。

### 优化
- 切换 agent 时排队消息按 agentId 隔离，不会误发到其他 agent。
- 发送失败的消息回退到排队队列并 toast 提示，避免丢失。
- 改善 restart 后历史会话加载：自动补全 sessionPath，loadMessages 失败重试。

### 修复
- 修复 agent 结束后排队消息无法触发 flush 的问题（agent_end 后同步推送 runtimeState 重置 isStreaming）。
- 修复 restart 后历史会话空白的问题。
- 修复启动时 get_commands 超时导致控制台报错的问题。

## v0.4.1 - 2026-06-03

### 优化
- 用户消息改为纯文本显示，避免特殊字符被 Markdown 解释导致渲染异常。
- 通知逻辑优化：只在会话结束时发送通知，工具调用时不触发。
- 工具调用期间保持 thinking bubble 动画显示。
- 隐藏项目列表的折叠箭头图标，界面更简洁。
- 减小项目列表左侧空白，布局更紧凑。
- 调整 agent 行关闭按钮位置，避免与边框重叠。

## v0.4.0 - 2026-06-02

### 新增
- 图片支持：从剪贴板粘贴图片（Ctrl+V）或拖拽图片到聊天输入框。
- 用户消息中显示图片，支持点击放大预览。
- 历史会话图片恢复：重新打开历史会话时正确显示之前发送的图片。
- 会话结束通知：agent 完成响应后发送系统通知（可在设置中开关）。
- 大图自动压缩：图片最长边压缩到 2000px，减少模型上下文占用。
- 发送图片到不支持图片的模型时显示错误提示。

### 优化
- 自动将 PNG/WebP 转换为 JPEG 传输，减少图片体积。
- 发送按钮支持仅图片无文字的消息。
- 历史会话加载时从 pi 会话文件中提取并显示图片。

### 修复
- 修复历史会话显示 thinking/reasoning 内容而不是实际回复的问题。
- 修复图片发送失败时无错误反馈的问题（现在显示错误信息）。
- 修复消息摘要中出现 ANSI 转义码的问题。

## v0.3.0 - 2026-06-02

### 新增
- 配置管理弹窗：点击侧栏滑块图标，查看和编辑 pi 全局配置文件（`models.json`、`auth.json`、`settings.json`）。
- Models 页签：可视化编辑器，provider 卡片 + 模型网格列表，支持新增/删除 provider 和模型，内联编辑 id、名称、contextWindow、maxTokens、reasoning。
- Auth 页签：按 provider 查看和编辑 API Key，支持新增/删除 auth 条目，Key 支持显示/隐藏切换和一键复制。
- Settings 页签：key-value 编辑器，自动识别类型（布尔复选框、数字输入框、复杂值 JSON 编辑）。
- 源文件页签：直接 JSON 编辑器，支持切换文件。
- 保存配置后自动 reload（对当前活跃 agent 调用 `agents.reload`）。
- 聊天输入框支持 `!command` 和 `!!command` 执行 bash 命令，与 pi 终端行为一致：`!` 执行并将输出发送给 LLM，`!!` 静默执行。
- Git 分支选择器现在同时获取本地和远程分支，新增分支数量徽章和空状态提示。

### 优化
- 所有 emoji 图标替换为 lucide-react 专业图标（Search、ChevronLeft/Right/Down、Play、Check、GitBranch、Eye/EyeOff、Trash2、Settings、Sliders）。
- 侧栏图标（配置管理 + 设置）使用 distinct lucide-react 图标，hover 高亮。
- Auth 和 provider 表单布局改为横向 label+input 网格，对齐更整齐。
- API Key 输入框在 Models 和 Auth 页签均支持显示/隐藏切换和一键复制。
- 分支下拉 z-index 和 overflow 修复，确保在 chat header 内正常显示。

### 修复
- 修复 chat header 的 Reload 按钮：之前把 `/reload` 当普通消息发给 LLM，现在调用专用的 `agents.reload` IPC 接口。
- 修复配置管理弹窗源文件页签：切换文件时重新加载对应文件内容，不再固定显示 `settings.json`。
- 修复分支下拉因父容器 `overflow: hidden` 被裁剪导致为空的问题。
- 修复 BranchSelector JSX 中的 stray tab 字符可能导致渲染问题。

## v0.2.2 - 2026-06-02

### 修复
- 修复打包后托盘图标丢失问题，使用 electron-vite 的 `?asset` 后缀确保图标路径在打包环境下正确解析。
- 修复设置页面在小屏幕下溢出视口的问题，添加最大高度约束和内容区域可滚动。

## v0.2.1 - 2026-06-01

### 修复
- 过滤聊天消息、工具详情和会话定位中的 ANSI 终端转义码。
- 会话定位默认显示最近 15 条，支持「显示全部」按钮展开完整列表；面板可滚动，最大高度 70vh。
- 会话定位摘要截断从 34 字符放宽到 48 字符，提升可读性。

## v0.2.0 - 2026-06-01

### 新增
- 历史会话重命名：右键点击会话卡片即可内联重命名（Enter 确认，Esc 取消），通过 JSONL 文件头部 sessionName 元数据持久化。
- 内置斜杠命令建议：输入 `/` 可看到 12 个 pi 内置命令（session、tree、clone、compact、copy、export、share、settings、reload、hotkeys、login、logout），与扩展注册的命令合并显示。

### 优化
- 过滤掉桌面端已有独立 UI 的冗余内置命令（/new、/model、/resume、/fork）。
- 移除 /name 命令，改用新的历史会话重命名 UI。

## v0.1.9 - 2026-06-01

### 新增
- 系统托盘支持：关闭窗口时默认隐藏到系统托盘，设置中可开关。
- 托盘右键菜单支持“显示窗口”和“退出”；Windows 下双击托盘图标恢复窗口。
- Agent 重启按钮：终止当前 pi RPC 进程并用同一 session 重新启动，适用于修改 provider/API key 等 `/reload` 无法生效的配置变更。
- Composer 工具栏新增手动上下文压缩按钮，context 占比超过 30% 时自动显示，带实时百分比和加载状态。
- 分支选择器从原生 `<select>` 改为自定义下拉，支持悬停高亮、当前分支标记和展开动画。

### 优化
- 精简 chat header 布局：更紧凑的间距、“New Session”渐变按钮、带过渡动画的操作组样式。
- Agent 加载期间隐藏分支选择器、会话操作和 composer，避免显示旧 UI。
- 点击历史会话时立即关闭抽屉，不再等待 agent 创建完成。
- 应用图标、侧栏、agent 头像、启动屏和空状态统一使用 pi 官方 wordmark logo。
- 上下文压缩按钮在压缩中使用黄色高亮，流式输出时禁用。

## v0.1.8 - 2026-06-01

### 优化
- 会话中的链接现在使用系统默认浏览器打开，不再在 Electron 窗口内导航。
- 切换项目时所有项目的 agent 列表默认展开显示；新增按项目折叠/展开切换。

## v0.1.7 - 2026-06-01

### 优化
- 缩小项目列表默认宽度，为会话区域留出更多空间。
- 优化项目搜索框和新增按钮布局，窗口变窄时新增按钮仍保持可见。

## v0.1.6 - 2026-06-01

### 优化
- 优化会话消息中的 Markdown 表格渲染，补充边框、间距、表头样式，并让宽表格安全横向滚动。
- 将不明显的 textarea 原生右下角拉伸改为输入框顶部可见拖拽条。
- 输入框拉伸增加高度边界，避免输入区域展开后挤占过多会话阅读空间。

## v0.1.5 - 2026-06-01

### 修复
- 调整会话页顶部布局，让较长项目路径和会话操作按钮在窄窗口下更稳定显示。

## v0.1.4 - 2026-05-31

### 新增
- 新增运行中 Agent 的停止 / 中断按钮，底层使用 pi RPC `abort`。
- 新增模型首个流式 token 到来前的“正在响应”动画。
- 新增工具调用聚合卡片，避免一次问答中的多个 tool 消息占满对话区域。
- 工具调用组默认只展示少量摘要，支持展开查看完整详情。

### 优化
- 工具调用详情默认折叠，内容较长时独立滚动。
- 运行中和失败的工具调用有更清晰的视觉状态。

## v0.1.3 - 2026-05-31

### 新增
- 新增启动时 pi CLI 环境检测，并通过可见弹框展示检测结果。
- 新增通用 pi 命令定位器，适配打包后的 Electron 环境。
- 设置页新增手动检测 pi 环境入口。
- 设置页新增当前版本显示和“检测更新”入口，点击跳转 GitHub Releases。
- 新增静态启动页，减少渲染进程加载前的白屏感。

### 优化
- 打包应用在窗口准备好后再显示，减少启动白屏。
- 项目列表改为延后异步加载，让主界面更快显示。
- pi CLI 检测覆盖常见 PATH、npm、pnpm、Yarn、Volta、mise、nvm、asdf、bun、deno 和本地 bin 目录。
- Windows 下 `.cmd` 形式的 pi shim 使用 shell 检测，避免“实际能打开 Agent 但检测失败”的误报。
- 未检测到 pi CLI 时，引导用户打开官方安装文档。
- 历史会话支持从父目录启动但内容中引用子项目时，归类到对应子项目下。

## v0.1.2 - 2026-05-31

### 修复
- 修复 `.pi`、`.pi-desktop` 等隐藏目录的项目头像只显示点号的问题，现在会跳过前导点和空白。
- 将 `downloads/` 加入 `.gitignore`，避免本地下载产物进入发布版本。

## v0.1.1 - 2026-05-31

### 新增
- 新增 Electron Builder 打包配置，支持 Windows、macOS 和 Linux 目标。
- 新增目录构建和平台分发构建脚本。
- 新增打包应用图标资源。

### 优化
- 补充 Linux 包维护者信息。

## v0.1.0 - 2026-05-31

### 新增
- 初始版本 PiStudio 工作台。
- 支持多项目桌面工作区，管理本地项目目录。
- 支持同时运行多个 pi RPC Agent。
- 支持历史会话抽屉和历史会话恢复。
- 支持文件抽屉、目录折叠和文件右键操作。
- 支持 Markdown 对话时间线和 Assistant 流式文本展示。
- 支持工具调用详情展示。
- 支持模型、思考等级、上下文和缓存状态显示。
- 支持 Git 分支展示和分支切换。
- 支持发送快捷键配置和面向桌面端的三栏布局。

### 修复
- 配置打包应用图标。
